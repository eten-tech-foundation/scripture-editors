{
  "$schema": "https://json-schema.org/schema",
  "$id": "MarkersData",
  "title": "",
  "type": "object",
  "properties": {
    "usfmStyleUrl": {
      "type": "string",
      "description": "URL or workspace-relative path to the USFM style file (vendored path preferred for deterministic regeneration)",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What is the URL or path of the USFM style file?"
    },
    "outputPath": {
      "type": "string",
      "description": "Where to save the resulting file",
      "default": "libs/shared/src/utils/usfm"
    }
  },
  "required": ["usfmStyleUrl"]
}
