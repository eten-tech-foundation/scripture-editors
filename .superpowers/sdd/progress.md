# SDD progress ledger — Standard view Phase 0+1 plan
Task 1: complete (commits a6d9b97..d108170, review clean; note: only platform+utilities have extract-api targets)
Task 2: complete (commits d108170..2aabcb9, review clean)
  Minor (for final review triage): redundant ' ' in regex class node.utils.ts:405 (plan-authored); single-letter segment cap noted as boundary
Task 3: complete (commits 2aabcb9..ffe09e4, review clean; 12/12 harness green)
  Plan corrections carried forward: Nx project name is @eten-tech-foundation/platform-editor (not "platform"); USX fixtures must be authored single-line where inter-element whitespace would become text nodes
  Minor (final review triage): findings-doc "Task 3 baseline run" narrative section deviates from finding-entry template shape (benign)
Task 4: complete (commits ffe09e4..057569a, review clean; 36/36, zero adaptor fixes needed — bridges/ca-cp-va-vp/ref/optbreak/ms/RTL all round-trip already)
Task 5: complete (commits 057569a..1ce5782, review clean; 56/56, acceptance criterion 5-of-5 lossless, vacuous-pass risk refuted by reviewer via converter source + scratch-script verification)
Task 6: complete (commits 1ce5782..54941d2, review clean; stale fixture corrected, circularity risk refuted by node-by-node trace)
  Minor (final review triage): book \id marker glyph absent from editable delta ops (pre-existing, confirm with collab adaptor owner); TODO aspiration to strip marker glyphs from ops = Phase 2 design question
Task 7: complete (commits 54941d2..3904573, review clean, zero findings; reverse adaptor needed no change; incidentally fixes latent caller-hidden-when-collapsed bug)
Task 8: complete (commits 3904573..1320947, 2 commits, approved after fix round)
  Fix round: plan's CSS assumed .marker class on MarkerNode — dead since commit 5ef9976 (#359); retargeted to .opening/.closing/.selfClosing + regression tests; class-drop recorded as phase-2-engine finding
  User-facing note: in-browser visual verification NOT performed (no Chrome for Playwright MCP in WSL, no sudo) — user should eyeball demo Standard view
Task 9: complete (commits 1320947..70a8b80, review clean; Unicode normalization verified already applied host-side at ScrText.PutText — spec §4 item resolved, no Phase 5 work needed)
Final whole-branch review (fable): READY TO MERGE. Important finding fixed in 23b4eaf (standard-view note delta ops pinned + comment fix + phase-2 follow-ups recorded); fix re-review approved, zero findings.
ALL 9 TASKS + FINAL REVIEW COMPLETE. Branch head: 23b4eaf.

# SDD progress ledger — Standard view Phase 2 plan (docs/superpowers/plans/2026-07-02-standard-view-phase-2.md, plan commit 784e6e6)
Task 1: complete (commits 784e6e6..717385d, review clean; note: test files need explicit .js import extensions for nodenext typecheck)
Task 2: complete (commits 717385d..1c7fe13, review clean; 14/14 + 124/124)
  Adaptations verified by reviewer: getMarker default import; NoteMarkerObject augments MarkerObject with closed?: string; regex classes behavior-preserving
  Minor (final review triage): dead CharFrame.container field (brief-verbatim); forward note — MARKER_OBJECT_PROPS whitelist could strip `closed` downstream, Task 3+ must confirm it survives serialization
Task 3: complete (commits 1c7fe13..60ed685, review clean; 23/23 + 133/133)
  Beyond-brief fixes verified: note-close now clears char stack; w/rb/jmp markers added via usfmMarkersOverwrites + getMarker overwrite-only path (generated file untouched)
  Minor (final review triage): getMarker overwrite-only guard checks only .type before `as Marker` cast; scanMilestone dead `between.includes("\\")` branch (plan-verbatim); milestone unmappable bare attribute silently dropped (plan-verbatim edge); note-close charstack tested only for note-internal spans
Task 4: complete (commits 60ed685..2887dc1, review clean; 137/137 + 1060/1060 + 176/176)
  Minor (final review triage): report cites TypedMarkNode.test.ts guard pattern imprecisely (guard placement differs; typecheck verified green regardless)
Task 4 fix round: closure-narrowing typecheck errors fixed in 6735763 (Task 4/5 "typecheck clean" claims were Nx cache hits — ALL later verification gates must use --skip-nx-cache)
Task 5: complete (commits 2887dc1..144d8a7, review clean; corpus 60/60 all modes)
  Beyond-brief fix verified correct: isCharChild separator strip ordering in reverse adaptor (provably cannot mis-strip: data NBSP pre-encodes to ~)
  Minor (final review triage): no focused unit test for isCharChild strip; para-leading rule untested in corpus; redundant isSerializedMarkerNode guard in createPara (plan-verbatim)
Task 6: complete (commits 6735763..28549f1, review clean; 10/10, platform 194/194, all gates cache-bypassed)
  Reviewer proved: FFFC count can only stay equal or decrease -> symmetry guard converts any tokenizer anomaly to zero-mutation refusal (no data-loss path); textType state survives $parseSerializedNode
  Minor (final review triage): multi-para caret drift after literal-\p split (cosmetic, untested); $requestTier2ForNode untested directly; rebuild regularizes whitespace runs (intended PT9 semantics)
Task 7: complete (commits 28549f1..22b1fbe, review clean; 8/8, platform 202/202)
  Deviations verified correct: local milestone check (MilestoneNode.isValidMarker z-wildcard would block \zed test); update-listener SELECTION_CHANGE self-dispatch (ScriptureReferencePlugin precedent, convergence airtight)
  Minor (final review triage): self-dispatch fires every commit while pendings non-empty — gate on anchor-key change would cut redundant broadcasts
Task 8: complete (commits 22b1fbe..25ca8a2, approved after fix round; 15/15, platform 209/209)
  Fix round: closer lookup mis-mirrored on collab-flattened nested spans -> marker-matched last-closer + parent-marker guard routing mismatched shapes to Tier 2; regression tests load-bearing
  Minor (final review triage): note-opener invalid renames no-op via $requestTier2ForNode note-scope skip (Phase 3 scope, pre-existing); isPara/isCharKindMarker duplicate shape
Task 9: complete (commits 25ca8a2..afc60ee, review clean; shared 138/138, platform 214/214)
  Adaptations verified (incl. Lexical-source trace of text-merge + caret survival): extraction test asserts merged-node outcome; regex NBSP escapes; _context rename
  Minor (final review triage): caret-after-extraction untested (depends on Lexical merge ordering); $chapterNodeTransform has zero direct test coverage (live registered code); dead conjunct in verse unterminated check (plan-verbatim)
Task 10: complete (commits afc60ee..42b04be, review clean; 6/6 new, platform 222/222)
  Adaptations verified: scoped jsdom Range polyfill (test-env artifact); tsconfig lib-exclude/spec-include mirrors shared/shared-react precedent; helper extraction complete
  Minor (final review triage): no-previous-para fallback can leave stray double-NBSP (plan-verbatim, cosmetic); merge test doesn't exercise the by-state orphan drop branch
Task 11: complete (commits 42b04be..504fd93, review clean; 5/5, platform 227/227)
  Beyond-brief loop-breaker verified: rebuildAttempted per-commit content-keyed dedup (only invariant across rebuilds; convergence proven; cleared before Task 7 self-dispatch)
  Minor (final review triage + Task 16 known-limitations): dedup can skip a would-succeed rebuild when two scopes share byte-identical trigger content in one commit — recovery needs a DIRECT content re-edit (Enter/blur do NOT retrigger terminated-skipped nodes; implementer report overstated self-heal); note-skip test partly vacuous (double-guarded)
Task 12: complete (commits 504fd93..6ffd0fc, review clean; 4/4, platform 231/231; @lexical/clipboard added as explicit dep)
  IMPORTANT record correction (for Task 16): §5.6 clipboard normalization is effectively UNREACHABLE in-app (Ctrl+C/ContextMenuPlugin/EditorRef.copy all dispatch null payloads; native context menu suppressed) — only out-of-band native ClipboardEvents engage it. Wire-or-accept decision deferred to Task 16/Phase 5. Task 15 QA check #9 must test copy via a path that actually works and note behavior.
  Minor: report's no-ping-pong justification imprecise (real mechanism: node detachment + adaptor pre-substitution); guard-rail-rejected rebuild + whitespace transform costs one extra rejected attempt (finite)
Task 13: complete (commits 6ffd0fc..382f982, approved after 2 fix rounds; 6/6, platform 236/236)
  Fix rounds: interior-range styled-plain-styled split (split-end-before-start ordering); in-span space reuse must still SPLIT (PT9 blank-style outcome) — plan's own test-3 assertion was the misleading culprit, rewritten structural
  Minor (final review triage): range end at offset===size unwraps whole span (over-conservative edge); benign Lexical DOMException warn in unwrap test; preventDefault before delegation on non-range ctrl+space
Task 14: complete (commits 382f982..b60bce8, review clean; 3/3, platform 239/239)
  Minor (final review triage): dirty-after-mount test docstring overstates re-render coverage (in-place text edits never recreate the classed element; created-mutation path is the real protection)
Task 15: complete (commits b60bce8..ae83555 [4], approved; whole-repo suites green cache-bypassed; browser QA 11/11 real-input PASS after two fix rounds)
  Fix 1: CommandMenuPlugin (sole purpose: block \ and / input) gated off when markerMode==="editable" (Editor.tsx) — engine-symmetric complement; scribe still mounts it unconditionally (latent if scribe gains editable modes)
  Fix 2: real-paste undo gap root-caused to ScriptureReferencePlugin dispatching SELECTION_CHANGE synchronously from a mutation listener mid-commit (advanced undo baseline past pre-paste state); fixed via queueMicrotask deferral (LoadStatePlugin precedent); regression test pins deferral ordering; browser-verified undo/redo single-step
  Minor (final review triage): gate test covers 2/4 view modes (unformatted untested); Editor.tsx gate comment omits that / is also unblocked; queueMicrotask unguarded on unmount (harmless, precedented); findings addendum dated 2026-07-03 vs session 2026-07-02 (cosmetic)
Task 16: complete (docs-only; no commit range — wrap-up). Final sweep re-run at branch head ae83555, all cache-bypassed: shared 138/138, shared-react 1060/2skip, platform 242/3skip, usfm-markers 19/19, utilities 3/3; lint/typecheck/format:check/extract-api all clean across 10 projects; git status --short empty (no API-report drift). Wrote: findings-doc "Phase 2 completion summary" (top of 2026-07-01-standard-view-phase0-findings.md); new 2026-07-02-standard-view-phase2-notes.md (Phase 3/4/5 handoff + known-limitations, corrected/expanded beyond the task-16 brief's seed content per progress.md's own record — clipboard gap severity, rebuildAttempted recovery semantics, CommandMenuPlugin/scribe latent gap, ScriptureReferencePlugin queueMicrotask fix, sentinel/refusal exact function list).
ALL 15 PHASE-2 TASKS + WRAP-UP COMPLETE. Branch head: ae83555 (docs commit for Task 16 lands after this line is written). Phase 2 engine is sound end-to-end (Task 15 browser QA, real keyboard/paste input, post-fix); two items deferred to Phase 5 with recorded candidate designs (clipboard wire-or-accept; Enter-menu). Phase 3 (footnote UX) and Phase 4 (StyleInfo) needs are enumerated in docs/superpowers/specs/2026-07-02-standard-view-phase2-notes.md with exact function-level swap/extension points.
Final whole-branch review (fable): initial verdict NO — 1 Critical (resolve/rebuild infinite loop on unterminated unknown marker + caret departure, empirically reproduced), 3 Important (caret hijack; unwrap/split attribute loss; reverse-adaptor viewOptions singleton -> recorded as Phase 3 task zero), + small batch.
Fix wave 1 (7506aac, b7e3c5b, d3af231): fixed-point refusal via structure-aware signature (deviation from literal-text check: verified it would wrongly refuse legit rebuilds); caret anchorInParas guard; attr suffix reconstruction on unwrap + left-half-only on split; batch items.
Re-review: signature/fragment asymmetries found — milestone runs (hang survived there) + undelimited glyph text (byte loss on refused restructures).
Fix wave 2 (c2a284c): $appendSignature mirrors fragment accounting (milestone run absorption + delimited marker glyphs); both revert-verified.
Final symmetry audit (opus): branch-by-branch exhaustive — READY TO MERGE. Transparent-wrapper asymmetry proven sound (mark removal is a real change; re-application async).
ALL 16 TASKS + FINAL REVIEW COMPLETE. Branch head: c2a284c. Platform 257/257(+3 skip), shared 138, shared-react 1060(+2 skip), all gates cache-bypassed. NOT PUSHED per instruction.

# SDD progress ledger — Standard view Phase 3 plan (docs/superpowers/plans/2026-07-02-standard-view-phase-3.md, plan commit a7ff027)
Part A = scripture-editors branch standard-view; Part B = paranext-core worktree branch standard-view off main (Tasks 8-13). Base before Task 0: a7ff027.
Task 0: complete (commits a7ff027..4eac995, review clean; task-zero test proves per-call viewOptions, forward adaptor untouched). Minor (final-review triage): recurseNodes viewOptions param placed 2nd not last — functionally correct, all call sites consistent.
Task 1: complete (commits 4eac995..1034914, review clean; rule mirrored byte-for-byte in createNote + $createWholeNote, corpus 64/64, closed round-trips via unknownAttributes). Minor (final-review triage): inline `MarkerObject & {closed?}` cast duplicates non-exported NoteMarkerObject shape; OT-collab insert path (delta-apply-update.utils.ts:1747) doesn't thread `closed` into $createWholeNote so collab-inserted unclosed notes render collapsed until reload (pre-existing, follow-up).
Task 2: complete (commits 1034914..d219371, review APPROVED by opus; noteContentRebuild 6/6, platform 266/3skip; all 6 named risks verified sound incl. 4 deviations from brief sketch — caller-as-TextNode structural content id, dropped unknownAttributes guard (safe: note preserved by identity), \p-prefix unwrap, fixed-point over content nodes). Deviations correct. Cascade integration-test gap CLOSED in Task 3. Minor (final-review triage): no caret-offset assertion for note-content rebuild.
Task 3: complete (commits d219371..5a5e06b initial + b9791b4 fix; re-review APPROVED, platform 271/3skip). Enter-in-expanded-note inserts \fp via manual splice (brief's insertNodes sketch was broken — Lexical escapes inline note to nearest block + glyph-less CharNode unwrapped same-commit; implementer used manual splice w/ real \fp opener glyph). Added Task-2 cascade regression test (C:\temp through real transform, no hang). Fix round: offset-0 empty-\ft cleanup was dead code (getChildrenSize never 0 due to opener MarkerNode) -> getChildren().every($isMarkerNode); +2 tests (offset-0 removal, collapsed-note fall-through). Helpers promoted to markerEdit.test-helpers.tsx.
Task 4: complete (commit c657644, review APPROVED; shared-react 1066/2skip, platform 271/3skip, extract-api drift committed). Data-driven callers: `-`->`*`, `+`->CSS-generated, custom->literal; cross-ref sequence (default †) via new @counter-style cross-ref-callers + crossRefCallers UsjNodeOptions. Used CSS class bucketing (.note.usfm_f/fe/ef/efe vs .note.usfm_x/ex) instead of a new node field — BOTH footnote and cross-ref increment rules scoped so no double-counting (reviewer verified no leftover unscoped [data-caller="+"] rule). Minor (final-review triage): expanded-state literal-caller path untested (pre-existing). Note: demos/platform vendored usj-nodes.css not touched (synced from paranext externally).
Task 5: complete (commit ea72e89, review APPROVED; shared-react 1074/2skip, platform 271/3skip, extract-api drift committed for 4 new UsjNodeOptions fields). PT9 snippet semantics: $stripSelectionToQuotation (new noteQuotation.utils.ts) strips markers+nested notes, verse->\+fv; $createNoteChildren uses chapterVerseSeparator + stripped quotation; default callers x/ex->defaultCrossRefCaller??"-", else defaultFootnoteCaller??"+". Deviations verified: nested-note skip needs ancestor-walk $isInsideNote (getNodes flattens); endpoint slicing needs $getCharacterOffsets (element-point .offset is child index). Untested-but-acceptable: verse-bridge separator formula (no UI builds bridge ref yet), visible-mode marker stripping (standard view uses editable). Minor: report imprecisely claimed a test-file call site (no functional impact).
Task 6: complete (commit ce6ba54, review APPROVED; shared-react 1077). Test-only: pins note-caller title=previewText tooltip (behavior already present). Scribe demo wiring skipped (optional). Part A engine+insertion (Tasks 0-6) COMPLETE.
Task 7: complete (bridge, no code review — verification+build). Whole-repo gates all GREEN cache-bypassed (test 9 proj, typecheck 10, lint 10/2-warn-0-err, format:check clean, extract-api 2, git clean no api.md drift). Browser QA smoke (demo localhost, Standard view, real Chrome/Playwright, 0 console errors): editor-input has marker-editable+text-spacing+formatted-font; markers render as editable text (\ide \h \toc1...); footnote -> note usfm_f collapsed with CSS-generated caller via FOOTNOTE counter (counter(caller,note-callers), NOT crossref — Task 4 bucketing confirmed live); caller title=preview (Task 6); Insert-footnote toolbar creates a note via engine (Task 5 path), note count 1->2, no error. yalc devpub OK: @eten-tech-foundation/platform-editor@0.8.15 published + linked into /home/lyonsm/paranext-core/node_modules (verified STANDARD_VIEW_MODE + new UsjNodeOptions fields present in pushed dist/index.d.ts); package.json restored. Part B consumes via editor:link in the worktree.
Part B SETUP (user chose Worktree+install): worktree /home/lyonsm/paranext-core-standard-view (branch standard-view off paranext main 68b5f29). Part B commits land in the WORKTREE, not scripture-editors.
ROOT CAUSE of the yalc bridge failure (found with user): paranext's `npm install` postinstall -> `link-dev-packages.ts` -> `checkoutRevision(scripture-editors, dev-packages.json revision)` which is pinned to `platform-yalc`, then pnpm install + `nx devpub` FROM platform-yalc + editor:link. So paranext always built the editor from platform-yalc (no standard-view work), left scripture-editors on platform-yalc, and pruned its node_modules (@lexical/clipboard absent on platform-yalc manifest). My wrong-branch `nx typecheck` then poisoned dist (.d.ts emit). NOT caused by devpub itself. progress.md is tracked-but-gitignored on standard-view, so any switch off standard-view deletes it.
RECOVERY (all work safe, tip b972079): git checkout standard-view; pnpm install (restored @lexical/clipboard, +2 pkgs); find dist -delete + delete *.tsbuildinfo; nx run-many typecheck build platform-editor --skip-nx-cache = SUCCESS (fresh dist has STANDARD_VIEW_MODE). FIX for the revert (Q1): set dev-packages.json revision -> "standard-view" in the worktree (local dev pointer; revert to platform-yalc when Phase 3 done). Q2: `npm run build` does NOT re-pull the editor — must `nx devpub` from standard-view to push new content to the yalc store. Note: harness LSP emits stale "missing export/@lexical/clipboard/describe" diagnostics during yalc churn — DISREGARD; single-project nx --skip-nx-cache gates are truth.
BRIDGE FIXED (steps 1-4 w/ user approval): committed ledger 5186626 (scripture-editors clean); dev-packages.json revision->standard-view in worktree (uncommitted local pointer); `pnpm -C packages/platform devpub` from standard-view pushed editor + AUTO-LINKED into worktree (line "Pushing ... in /home/lyonsm/paranext-core-standard-view"); scripture-editors stayed on standard-view (devpub does NOT switch — only paranext npm install does). Verified worktree link now has STANDARD_VIEW_MODE + crossRefCallers; extension baseline typecheck green. Part B commits are in the WORKTREE. Reviews use `git -C worktree diff <base>..<head>` (review-package is scripture-editors-only).
Task 8: complete (worktree commits c57032b initial + 1aca388 fix; review by sonnet found CRITICAL then re-verified). Added 'standard' to ScriptureEditorViewType + getViewOptionsForType (getViewOptions(STANDARD_VIEW_MODE)); isReadOnlyEffective unchanged (standard editable). Fix: changeScriptureView cycle was keyed on markerMode -> stuck standard<->markers in non-power mode (both formatted+markers are markerMode hidden there), orphaning formatted; switched cycle to key on viewType directly (formatted->standard->markers->formatted). typecheck PASS. Pre-existing non-power markers-display asymmetry (markerMode stays hidden) left as-is (out of scope). Verified Part B code-review CATCHES runtime state-machine bugs typecheck can't.
Task 9: complete (worktree commit a9166582, review APPROVED; extension typecheck re-verified PASS by controller — harness LSP diagnostics on isNewNote/platform-bible-react were STALE). FootnoteEditor popover already runs the marker engine on note content (Editorial mounts MarkerEditPlugin when markerMode editable; popover inherits markerMode from host options.view, only overrides noteMode->expanded — verified chain web-view options memo -> editorOptions -> footnote-editor.component.tsx:219). Added isNewNote prop + selectNote(0) caret-after-\ft for new notes (reuses $selectNote, view-agnostic, safe). Rebuilt+committed lib/platform-bible-react/dist (TRACKED in git; extension consumes via file: dep) — the 6512-line bundle churn swept in PRE-EXISTING lib-src drift from 4 prior commits that never rebuilt dist (accepted; now synced; future lib rebuilds will be small). LIMITATION (accept-documented, Phase 4/5): popover forces noteMode:expanded so getViewMode != STANDARD_VIEW_MODE -> isStandardView() false in popover -> §4 whitespace-display + copy/cut NBSP-normalization inactive on note content (core Tier1/2 marker editing unaffected, gates on markerMode). Fixing needs a scripture-editors isStandardView/getViewMode change. Revisit if Task 13 QA shows glyph leakage on note-text copy.
Task 10: complete (worktree commit 113f5445, review APPROVED zero issues; 5 files +180, typecheck PASS). Footnotes-pane auto-show/hide behind setting `footnotesAutoShow` (useWebViewState default FALSE = PT9 unchanged). When ON + viewType==='standard' + !override: setFootnotesPaneVisible(chapterHasNotes). chapterHasNotes via same UsjReaderWriter(usj,{markersMap}).findAllNotes() as FootnotesLayout. Override: manual toggle sets footnotesAutoOverrideRef=true before flip; chapter-change effect (scrRef.book/chapterNum) resets. Full toggleFootnotesAutoShow wiring mirrors toggleFootnotes (menus.json order3 + en/es localized key + main.ts cmd reg+handler + controller message + web-view listener). chapterHasNotes/auto-effect placed after usjFromPdp (line ~1497) to avoid TDZ (verified). Runtime deferred to Task 13.
Task 11: complete (worktree commit b6fad5df, review APPROVED; 3 files +102/-16, typecheck clean). Caller-click branches on footnotesPaneVisibleRef: VISIBLE -> highlight pane row + return (no popover); HIDDEN -> popover (unchanged). Deviation (verified sound): editorRef.selectNote only moves editor-body selection, NOT pane highlight -> added focusRequest prop to FootnotesLayout + findNoteIndexByOps helper (utils.ts) to drive same setSelectedFootnote({footnote,index}) a real row-click uses; index basis = $getNoteByKeyOrIndex $dfs order == pane's findAllNotes pre-order (aligned). Minor (final-review triage, FIX BEFORE MERGE candidate): footnotes.component.tsx:95-102 sets lastAppliedFocusRequestRef BEFORE bounds check -> a click during pane mount window silently fails to highlight (self-heals next click; one-line fix = move ref assignment after bounds check; auto-show makes it slightly more reachable). Minor: findNoteIndexByOps O(notes×doc) per click (fine typical). Known-limitations (Task 13 runtime): no scroll-into-view for off-screen pane row; content-equality ambiguity for byte-identical duplicate notes (pre-existing pane behavior).
Task 12: complete (worktree commits aa1e66d1 initial + 3608774 fix; review APPROVED w/ 1 Important fixed, typecheck PASS both times [re-verified by controller — platform-bible-react "no declaration" LSP diagnostics after lib rebuild are STALE]). Added nodeOptions snippet fallbacks (chapterVerseSeparator ':', verseRangeSeparator '-', defaultFootnoteCaller '+', defaultCrossRefCaller '-', crossRefCallers ['†'] + TODO(phase5); values == library defaults so inert until Phase 5 wires real project settings). Context-menu "Insert footnote"/"Insert cross-reference" + Ctrl+T/Ctrl+Shift+T shortcuts -> reused existing insertFootnote/CrossRefAtCurrentSelection -> editorRef.insertMarker('f'/'x') -> host openFootnoteEditorOnNewNote popover (Phase-3 snippet path confirmed). Shortcuts hooked into existing keydown effect, scoped activeElement===.editor-input (no popover leak), gated !isReadOnlyEffective && viewType==='standard'. Ctrl+K omitted (no host command). FIX (Important): FootnoteEditor popover inherited main-editor contextMenu extras whose onSelect edit the MAIN doc -> added contextMenu:undefined in footnote-editor.component.tsx options memo + rebuilt lib dist. Minor (final-review triage): Ctrl+T no Mac Cmd+T branch (matches pre-existing Ctrl+F precedent; verify on macOS); editorInput querySelector captured once/effect (pre-existing staleness); context-menu inserts ungated to standard (harmless, isReadOnlyEffective covers markers view). ALL PART B IMPL TASKS 8-12 DONE.

FINAL WHOLE-BRANCH REVIEWS (opus): Part A (a7ff027..ce6ba54) READY TO MERGE — note-scope loop verified terminating, cross-task types consistent; Important non-blocking fast-follow: OT-collab delta-apply-update.utils.ts:1747 doesn't thread `closed` (collab-inserted unclosed notes collapse until reload, no data loss). Part B (68b5f29..3608774) WITH FIXES — caught NEW cross-task bug: changeScriptureView effect deps still had vestigial viewOptions.markerMode (omit viewType) -> cycle jams on formatted in non-power mode. FIXED (worktree d206e992): deps viewType; + free focusRequest bounds-check reorder. Extension typecheck green.

RUNTIME QA (Task 13) via paranext app-runner + visual-verification skills (read manually, not registered; refresh.sh headless xvfb CDP:9223, pw-server.mjs over CDP): CONFIRMED in Platform.Bible standard view: standard view renders (inline editable markers + tooltips); view cycle formatted->standard->markers works (live-validates Task-8 deps fix); menu has Switch Scripture view + Auto-show Footnotes Pane + Insert footnote/cross-reference; insert footnote -> caller + FootnoteEditor popover opens (task-zero: 2 Editorial instances coexist, no corruption); pre-existing notes render with content+callers.
BUG FOUND + FIXED (cc82802, review APPROVED): inserting a footnote produced EMPTY note (\f \f*, no \fr/\ft). ROOT CAUSE: $createNoteChildren built \fr/\ft char spans WITHOUT opening MarkerNode glyph -> in editable markerMode, $charNodeDeletionTransform/$unwrapCharNode unwrapped them to plain text same-commit (SAME class as Task 3 \fp bug). Task 5 unit tests missed it (didn't mount MarkerEditPlugin transforms). FIX: new $createNoteContentChar builds spans matching reverse adaptor createChar (opener glyph + NBSP prefix) when editable; viewOptions threaded through $createNoteChildren; other modes unchanged. New noteInsertion.test.tsx mounts MarkerEditPlugin + reproduces (fails pre-fix). platform 273/3skip, shared-react 1075/2skip. RE-DEVPUB NEEDED to reach Platform.Bible (fix in libs/shared-react). Env lesson: paranext app uses webpack DEV cache (node_modules/.cache/webpack-extensions); yalc keeps version 0.8.15 so webpack serves STALE editor bundle across re-publish -> MUST clear that cache + refresh.sh after devpub. Minor (fix review): no \fq/\xq quotation-branch test (collapsed selection); popover markerMode override not located (no regression).

PROPAGATION "BLOCKER" RESOLVED (2026-07-03) — CORRECTS the line above's "stale bundle" claim. The reported empty-note-in-app was NOT a stale build. Marker-bisection (unique string literal planted in $createNoteChildren, traced by node -e fs.readFileSync().match()) proved all 4 hops propagate: src->platform/dist (probe present) -> yalc node_modules after devpub (present; node_modules already held the fix build size 390877 at session start) -> extensions/dist/.../main.js after build:extensions (present, editor embedded ~3x as sourcemap/eval strings) -> RUNNING APP (probe fired in-app console, logging vo={markerMode:"editable",noteMode:"collapsed",...}). Two red herrings caused the false alarm: (1) THE VERIFICATION WAS WRONG — QA counted .char DOM nodes on the LAST +-caller note in document order in the MAIN editor, but standard-view notes render noteMode:collapsed (content hidden inline, in popover) AND that note was a stale empty leftover, not the just-inserted one. Correct check = editor STATE (root.__lexicalEditor.getEditorState().toJSON()), before/after diff of +-notes with char/fr+char/ft children. State-checked, the insert yields [marker/f, immutable-note-caller, text, char/fr[fr," 1:3 "], text, char/ft[ft," "], text, marker/f] — CORRECT. (2) webpack --watch does NOT re-emit on a yalc same-version (0.8.15) swap (byte-identical output/frozen mtime); a FRESH npm run build:extensions (watch stopped) DOES re-bundle — clearing webpack cache was NOT the fix. RELIABLE RUNBOOK: nx build platform --skip-nx-cache; pnpm -C packages/platform devpub; (worktree) npm stop; npm run build:extensions; ./.erb/scripts/refresh.sh. Runtime-confirmed clean (probe removed) via state + a popover screenshot showing \f + \fr 1:2 \ft \f*. Full writeup: docs/superpowers/2026-07-03-paranext-propagation-blocker.md (commit 982df1a). Source == cc82802 (probe reverted, tree clean). NO real propagation risk for later phases.
PHASE 3 COMPLETE: Part A (scripture-editors standard-view, Tasks 0-7) + Part B (paranext worktree standard-view, Tasks 8-13) implemented, reviewed, unit-verified, and RUNTIME-CONFIRMED in Platform.Bible. Deferred non-blocking fast-follows: OT-collab `closed` threading (delta-apply-update.utils.ts:1747); \fq/\xq quotation-branch test; footnote popover whitespace/copy NBSP rules (needs scripture-editors isStandardView/getViewMode change); Mac Cmd+T; footnotes.component.tsx focusRequest bounds-check reorder (one-line, fix-before-merge candidate).

# SDD progress ledger — Standard view Phase 4 plan (docs/superpowers/plans/2026-07-03-standard-view-phase-4.md, plan commit f60d1a2, spec d0c52f3)
Task 1: complete (commit f60d1a2..c844382, review APPROVED, 5/5 tests). Minor (recorded): spec doc's StyleInfo sketch comment says "raw int /50 -> vw" for indent/margins; authoritative convention is .sty INCHES (x20 -> vw), per plan T8/T10 and styleInfo.ts doc — fix spec comment in a later docs commit.
Task 2: complete (c844382..e55e4aa, review APPROVED, shared 148/148). Deviation (verified safe): parser strips "#!" USFM3-only prefix — vendored sty's 7 milestones were all #!-prefixed; audited no other switch-cased directive affected. Minor (recorded for final review): (1) README "uncategorized-excluded" wording misleading; (2) schema.json usfmStyleUrl description still URL-only; (3) 4 new parser fields (fontName/lineSpacing/subscript/notRepeatable) + no-StyleType skip branch unexercised by vendored sty or tests.
Task 3: implemented (a252e90) but review = NEEDS FIXES. Critical: forced isNested:true for unknown-in-note chars violates PT9 (auto-close unconditional for USFM2/3; would serialize typed \zfoo as \+zfoo) — plan's test expectation was authorship error, PT9-parity clause governs. Important: z-narrowing gate !startsWith("z") contradicts comment; reuse isKnownMilestoneMarker suffix pattern (+ isMilestoneCommentMarker). Fix dispatched to implementer agent. Minor (recorded for whole-branch review): PRE-EXISTING end-token laxity vs PT9 — unmatched closer leaves charStack intact (placement stays inside open span) and +-stripped findLastIndex lets bare \zfoo* close a nested \+zfoo (usfmFragmentToUsj.ts ~356-372).
Task 3: complete (e55e4aa..7fcb419 = a252e90 impl + 7fcb419 review fixes, re-review APPROVED, shared 156/156, tokenizer 31/31). Fixes: unknown-in-note isNested from + prefix (PT9 flat-sibling auto-close; \+zfoo byte-invention gone); isMilestoneHeuristicName suffix gate (mirrors isKnownMilestoneMarker); header comment. Minor (recorded for final review): (1) "unconditionally" comment should carry "(USFM 2/3; 3.1 differs)" qualifier; (2) neutral: \*-terminated wildcard-only z-marker no longer milestone-scans (more PT9-faithful); (3) carried: pre-existing end-token placement/matching laxity vs PT9.
Task 4: implemented (5dd708f), review = NEEDS FIXES (1 Important: \zzz+Enter test weakened to substring \\zz — must assert deterministic post-Enter shape; minors: fixed-point test node-identity discriminator, stale rebuildAttempted comment in markerEditTier2Trigger.utils.ts:37, optional routing-test strengthening). Fix dispatched to implementer. Implementer found+fixed latent pre-existing bug: $rebuildNoteContent called tokenizer with no options (masked by guard refusal). Minor (recorded for final review): duplicate zq/zfoo relaxed-guard coverage in tier2Rebuild tests (plan-mandated overlap).
Task 4: complete (7fcb419..dc1d036 = 5dd708f impl + dc1d036 test/comment fixes, re-review APPROVED, platform 278/3skip). Threading complete (30 call sites verified context-form), stylesheet-first guards, isNoteContext threaded, latent $rebuildNoteContent no-options bug fixed. Minor (recorded for final review): duplicate zq/zfoo relaxed-guard coverage in tier2Rebuild.utils.test.tsx (collapse candidate).
Task 5: complete (dc1d036..7b626c1, review APPROVED, gates green, api.md scoped). @public tags added to styleInfo.ts (doc-only, sanctioned). FAST-FOLLOW assigned to Task 9: export Marker/MarkerType/CategoryType from platform index + @public tags in usfmTypes.ts to clear the baked ae-forgotten-export:Marker warning in api.md (reviewer recommendation, do before Task 7 makes MarkerLookup consumer-facing if convenient).
Task 6: implemented (2396630), review APPROVED but controller adjudicated 2 must-fix items before T7: (1) SPEC/PLAN AUTHORSHIP ERROR found by reviewer vs PT9 source — TagValidator.cs:28-30 returns valid WITHOUT pushing for empty-occursUnder tags (incl. auto-created unknowns) when stack non-empty; plan's port pushed. PT9-parity clause governs; fix dispatched (no-push semantics + discriminating tests incl. reviewer's c->x->s1 counterexample). (2) tautological "unknown joins stack" extra test replaced. Also adding rank-forbidden-continues-scanning discriminator. SPEC DOC FIX NEEDED (Task 13 docs commit): spec §Validation "unknown ... participates in the stack with empty occursUnder" line — correct to PT9 no-push semantics; also the earlier-recorded indent-units comment fix.
Task 6: complete (7b626c1..588f37a = 2396630 impl + 588f37a no-push fix, re-review APPROVED, 19/19 file, platform 297/3skip). Port now branch-for-branch TagValidator.cs (verified vs source); 3 counterexample-grade discriminator tests. Minor (recorded): case-11 xq test name overclaims (brief-prescribed shape); spec-doc amendment queued (already in Task 13 list).
Task 7: complete (588f37a..124a802, review APPROVED clean, 5/5 new, platform 302/3skip). Substitutions sanctioned+verified (Harness rerender for live styleInfo change; $initial load route). Minor (recorded): pre-existing bare .status_unknown/.status_invalid rules at usj-nodes.css:2299-2307 now redundant dead weight (identical values, lower specificity) — consolidation candidate; per-pass debug log chattiness (plan-mandated verbatim).
Task 8: implemented (54e56bc), review APPROVED with 1 Important (test-only): ~half of formula branches unasserted (rightMargin, subscript, lineSpacing 2/else, italic, underline, fontName, justification rtl-swap) + double font-size cascade interaction uncommented. Fix dispatched (tests + one comment). Note: fontName row missing from brief's table but present in brief's code (carried faithfully).
Task 8: complete (124a802..82c55a5 = 54e56bc impl + 82c55a5 coverage fix, controller-verified test+comment-only, 5/5 file, platform 307/3skip). All formula branches now exact-string covered incl. rtl swaps + double font-size cascade.
Task 9: complete (fast-follow f77e808 + no code fixes needed for gates). Fast-follow: Marker/MarkerType/CategoryType exported from platform (@public tags in usfmTypes.ts + template, value/type exports in packages/platform/src/index.ts); api.md ae-forgotten-export warning gone, verified the unrelated pre-existing tsdoc-escape-right-brace warning is unaffected (reproduced with/without the fast-follow). Whole-repo gates ALL GREEN cache-bypassed, zero fallout fixes needed: test (9 proj: shared 156/156, shared-react 1075/2skip, platform-editor 307/3skip, utilities/perf-react/etc.), typecheck (10 proj clean), lint (0 errors/43 pre-existing warnings), format:check clean, extract-api (2 proj, only fast-follow's intentional diff), git status clean (no api.md drift). Demo browser QA (Playwright MCP, real Chrome, Standard view preset): confirmed the StyleInfo/MarkerValidationPlugin feature itself works — \zfoo-class unknown marker -> status_unknown/bold/rgb(204,30,20); \ft in body -> status_invalid/red border-bottom; renamed valid marker (q2->s1->s2, no \s1 existed in fixture so created via in-place opener rename) -> no status classes. BEYOND-BRIEF FINDINGS (both pre-existing Phase 2/3 Tier1/Tier2 marker-editing engine, NOT Phase 4 regressions, not fixed per task charter): (1) mid-paragraph typed-marker caret scrambling — general, reproduces for any unrecognized letter (z, y tested), caret restores BEFORE not after the split-triggering character; (2a) caret departure from a scrambled-marker paragraph crashes $rebuildParas's $parseSerializedNode ("frozen Lexical node map"); (2b) MORE CONCERNING, broadly reachable: renaming ANY existing marker opener then moving the caret AT ALL (click or single ArrowRight) crashes $applyOpenerRename's ParaNode.setMarker (frozen-node-map/_cachedNodes read-only) via MarkerEditPlugin's deliberate synchronous SELECTION_CHANGE_COMMAND self-dispatch from its own updateListener, leaving the doc in a visually-inconsistent state (new glyph text, stale paragraph class) — recommend a dedicated fix task before Phase 4 merges to main. Root-caused via git log (markerEditTier1.utils.ts last touched by Phase 4 Task 4's 5dd708f for MarkerLookup threading only, NOT the self-dispatch structure, which is Phase 2 commits 22b1fbe/6cd7594/25ca8a2) — likely never browser-QA'd for this exact "rename existing opener then move caret" interaction before. PHASE 4 PART A (Tasks 1-9, library StyleInfo integration) COMPLETE.
Task 9: DONE_WITH_CONCERNS (f77e808 exports fast-follow + 52b6b65 ledger; gates ALL GREEN cache-bypassed, zero fallout; demo QA: status_unknown/status_invalid CONFIRMED live in Chrome w/ exact colors). TWO PRE-EXISTING Phase 2 engine bugs root-caused during QA (task-9-report.md:83-162): (A) rename-existing-opener + caret-move reliably throws (dev freeze guard) via MarkerEditPlugin post-commit SELECTION_CHANGE self-dispatch -> $resolvePendingMarkers -> setMarker getWritable on frozen committed selection; rename does NOT complete (DOM keeps old class). (B) mid-paragraph typed-marker caret scrambling -> scrambled shape crashes $rebuildParas round trip ($parseSerializedNode frozen node map). MarkerValidationPlugin verified innocent. INSERTED Task 9b (unplanned, required for Task 13 QA to pass): fix both in Phase 2 engine code.
Task 9: review APPROVED (both commits; trailers controller-verified 2/2). Checklist item 3 "restyles" gap acknowledged NOT silently passed — it IS Bug A, fix in flight as Task 9b. Part A CLOSED except 9b.
Task 10: complete (worktree d206e992a34..b55ec7f256a, review APPROVED, C# build clean, 1297/6skip/0fail full suite, 8 new StyleInfo tests vs real DummyScrStylesheet + wire-surface assertion). Deviations verified sound: defaultFont/Size via scrText.Language.FontName/FontSize (ScrText.DefaultFont absent in ParatextData 9.5.0.22; matches CSSCreator.cs:98-100); TextProperties enum fully qualified (name shadowing); per-property JsonIgnore WhenWritingNull (no global policy; PlatformNotification precedent). Wire shape probe-verified (camelCase, raw dict keys, null omission, inches, #RRGGBB uppercase). Trailer verified (+ Task 11's, below).
Task 11: complete (worktree b55ec7f256a..a9b09642975, review APPROVED, tsc clean, trailer verified by reviewer). All 24 MarkerStyleInfo fields exact; both registration points. Minor (recorded): stray space "leftMargin/ rightMargin" in reflowed JSDoc (one-char cosmetic).
Task 9b: complete (52b6b65..d037693, review APPROVED, platform 311/3skip, whole-repo gates green, browser-verified: rename restyles + no crash, \zfoo unscrambled, all Task-9 checklist items pass). Root cause: Bugs A+B crashes = ONE mechanism (sync SELECTION_CHANGE self-dispatch short-circuiting into state frozen by OnSelectionChangePlugin's editor.read force-flush); fix removes the handler, defers resolution via queueMicrotask -> fresh top-level editor.update (ScriptureReferencePlugin precedent), with resolveQueued dedup + disposed guard; termination = no-commit->no-listener->no-requeue + fixed-point refusal. Scrambling = separate $rebuildParas raw-vs-cumulative span-offset conflation, fixed as strict generalization. Minor (recorded for final review): (1) except-key can degrade to undefined if selection becomes non-Range between schedule and microtask (doc/code tension vs lines 219-222; matches BLUR behavior); (2) per-commit array spread in schedule guard (alloc nit); (3) 9b implementer flags: OnSelectionChangePlugin editor.read-in-handler latent hazard (follow-up), CLAUDE.md Lexical version stale (0.33.1 vs 0.43.0). PART A FULLY CLOSED.
Task 12: implemented (6bc22ad27ec), review = 1 Critical (unused StyleInfo import in web view — passes webpack, fails noUnusedLocals/eslint gates; reviewer verified extensions/tsconfig.json:36 + .eslintrc.cjs:111). All other requirements verified exact incl. popover options-spread inheritance (styleInfo flows automatically, no lib change). Fix dispatched: annotate styleInfo memo with platform-editor StyleInfo type (doubles as structural-compat assertion between d.ts and editor types).
Task 12: complete (a9b09642975..54bf9155fc8 = 6bc22ad27ec impl + 54bf9155fc8 one-line annotation fix, controller-verified diff + trailer; typecheck clean, lint error gone stash-verified, build:extensions clean, StyleInfo structural compat between d.ts and editor types CONFIRMED by annotation compiling). Popover inherits styleInfo via options spread (verified vs source). devpub fanned out to worktree node_modules directly.
Task 13: complete (runtime QA 6/8 PASS; docs commit this entry rides in). Propagation clean per runbook: lib rebuild --skip-nx-cache -> devpub (byte-identical in worktree node_modules, Phase 4 markers present) -> npm stop -> dotnet build ParanextDataProvider.csproj (Debug DLL fresh, StyleInfo strings present) -> fresh build:extensions (bundle has generateUsjCss/useProjectStylesheet/platformScripture.StyleInfo) -> refresh.sh headless CDP:9223. Test fixture: wgPIDGIN custom.sty REPLACED with brief content (original kept as custom.sty.phase4-original.bak); DefaultFont temporarily Andika for item 2 discriminability (restored to Charis SIL after). PASS: (1) s1 red merge (generated rule keeps bold/size/spacing + gains #FF0000; computed rgb(255,0,0)/700); (3) \zln text\zln* in \p -> char/zln [opener glyph, " text", closer glyph], rgb(0,0,255)/700, no status classes (stylesheet-first beat z-wildcard); (4) \zfoo -> para split, glyph "opening status_unknown" rgb(204,30,20)/700; (5) \ft in body -> "opening status_invalid" red + red border-bottom, real in-note char/ft undecorated; (6) \zln in s1 -> both glyphs status_invalid, s1 glyph clean; (8) 0 console errors all session (error/unhandledrejection hooks), 30-char burst 8ms with validation active. FAIL: (2) project default font — base rule IS injected (.editor-input{font-family:"Andika";font-size:18pt}) but static .usfm.formatted-font (0,2,0) outranks the (0,1,0) base selector; per-marker rules tie (0,2,0) and win by order — one-liner candidate: stronger containerSelector for the base rule. FAIL: (7) FootnoteEditor popover — NO Tier-1/Tier-2 resolution at all (markers stay literal text at both note-content and para level; same beforeinput method resolves <2s in main editor; popover class list IS marker-editable; no errors) — needs dedicated debug (suspect: Tier-2 rebuild guard vs popover fragment doc shape). QA-method finding: raw "\" never reaches Lexical in-app (extension keydown preventDefault -> menu w/ focus-stealing search input; FootnoteEditor same via its own trigger; fast-typed keys can race into the doc — observed+undone "azln n" corruption); items 3-6 driven via execCommand insertText (native beforeinput == typing downstream of keydown). Observations: caller-click (pane hidden) did not open popover this run (used Ctrl+T path, works incl. Phase-3 note shape); popover Cancel/deleteIfNew left the new note (undone). All QA artifacts undone in-app; SFM on disk clean (0 remnants); app stopped; Settings.xml restored. Docs: phase4 notes (docs/superpowers/specs/2026-07-04-standard-view-phase4-notes.md) + 2 spec corrections in the design doc (indent/margin units ".sty inches ×20×zoom -> vw" not "raw int ÷50"; TagValidator.cs:28-30 empty-occursUnder valid-WITHOUT-push unless stack empty). Full report: .superpowers/sdd/task-13-report.md.
PHASE 4 COMPLETE: Part A (scripture-editors f60d1a2..d037693, Tasks 1-9+9b) + Part B (worktree d206e992a34..54bf9155fc8, Tasks 10-12) implemented, reviewed, unit-verified, runtime-QA'd in Platform.Bible (6/8; item-2 base-rule specificity and item-7 popover marker-editing carried to Phase 5 as fast-follows alongside menu/filtering-API, formatted-view CSS, zoom wiring, OnSelectionChangePlugin editor.read hazard, CLAUDE.md Lexical version).
Task 13: DONE_WITH_CONCERNS (docs commit 7e073da: notes doc + 2 spec corrections + ledger; QA 6/8 PASS in Platform.Bible, project wgPIDGIN, state+computedStyle verified; all core StyleInfo features live: s1 merge red, zln char blue/bold stylesheet-first, zfoo para-split status_unknown, ft status_invalid, zln-in-s1 invalid, 0 console errors, 8ms/30char latency). FAIL item 2: injected base font rule (.editor-input, (0,1,0)) loses to static .usfm.formatted-font (0,2,0); per-marker rules unaffected. FAIL item 7: marker editing INERT in FootnoteEditor popover (markers literal, no engine, \ swallowed by popover menu trigger) — popover options override (view/hasExternalUI/markerMenuTrigger) suspected of dropping markerMode / mounting the \-menu. INSERTED Task 13b: fix both + re-propagate + re-verify in-app. Other observations (Phase 5 handoff, in notes doc): caller-click hidden-pane branch untested this run; popover Cancel left note; in-app literal \ keystroke race with menu interception.
Task 13b: PARTIAL (item 2 FIXED+re-verified; item 7 root-caused -> NEEDS_CONTEXT, no fix attempted per charter). Fix A (2bd0f05): generateUsjCss default containerSelector ".editor-input" -> ".editor-input.usfm" (base rule (0,2,0) ties static .usfm.formatted-font, wins by injection order; per-marker rules -> (0,3,0)); TDD (4 tests red then green), platform 311/3skip, lint+extract-api clean (no api.md drift). Re-propagated per runbook (build --skip-nx-cache -> devpub byte-identical 548037 -> npm stop -> fresh build:extensions, ".editor-input.usfm" x3 in main.js -> refresh.sh) and re-verified IN-APP (wgPIDGIN, DefaultFont temp Andika, restored): item 2 PASS — injected base rule .editor-input.usfm{font-family:"Andika";font-size:18pt}, computed Andika/24px(=18pt). Regressions PASS: s1 rgb(255,0,0)/700/center; \zfoo -> para split [marker/zfoo," ","an da world."] glyph "opening status_unknown" rgb(204,30,20)/700; undo restored (0 zfoo nodes, verse text exact); 0 console errors; SFM on disk clean; app stopped. Item 7 DIAGNOSIS (headless repro in scratch test + Task 13 qa7.png BOTH confirm): NOT the options override — FootnoteEditor memo spreads editorOptions.view and only overrides noteMode:"expanded" (source==dist==Phase 3 claim; popover classes marker-editable; engine mounts). ROOT CAUSE: editable-markerMode delta round-trip malforms the popover note — getNoteOps serializes char-span glyph MarkerNodes (\fr,\ft) + closing \f* as text ops (only note's FIRST text child skipped, editor-delta.adaptor.ts:216-220); popover applyUpdate re-synthesizes glyphs -> DOUBLED glyphs + ImmutableUnmatchedNode from bare \f* op (qa7.png shows "\f + \fr \fr 1:1 \ft \ft\f*...\f*"); Tier-2 note rebuild aborts "sentinel/preserved-node count mismatch" -> literal settle. Para-level separately inert: popover para's only editable text is the \p glyph marker-trailing-space node, skipped by $textNodeTier2Transform by design. \-swallow = FootnoteEditor's own doc-keydown menu trigger (footnote-editor.component.tsx:504-529). Candidate fix (NOT attempted, OT-collab blast radius; also affects popover-Save writing the malformed shape into the MAIN doc via replaceEmbedUpdate): canonical glyph-free serialization in $handleTextNodes. Worktree untouched (HEAD 54bf9155fc8, no commits). Report: .superpowers/sdd/task-13b-report.md.
Task 13b: Fix A COMPLETE (2bd0f05 generateUsjCss containerSelector .editor-input.usfm + 9140dad docs; item 2 re-verified PASS in-app: Andika 18pt computed on container; regressions pass, 0 console errors). Fix B STOPPED AT DIAGNOSIS (correctly, NEEDS_CONTEXT): popover DOES preserve markerMode (engine mounts; Phase 3 claim + Task 12 review both right); REAL cause of item-7 inertness = PRE-EXISTING editable-mode delta round-trip defect: getNoteOps serializes glyph MarkerNodes + closing \f* as text ops (editor-delta.adaptor.ts:216-220, only first child skipped), applyUpdate re-synthesizes glyphs -> doubled glyphs + ImmutableUnmatchedNode -> note Tier-2 rebuild aborts (sentinel mismatch) -> literal settle. ESCALATION FLAG: popover Save may write the malformed doubled-glyph shape into the MAIN document today (OT-collab blast radius). Phase 3's popover screenshot actually shows the malformed shape (evidence-reading lesson). \-swallow = FootnoteEditor's own keydown menu trigger (gate pointless until adaptor fixed; not committed). AWAITING USER DECISION on adaptor fix scope. Final whole-branch review dispatched.
FINAL WHOLE-BRANCH REVIEW (fable): READY FOR MERGE CONSIDERATION (popover defect excluded as user-gated). No Critical/Important. Cross-task coherence verified (seam semantics, no-push consistency, termination net incl. unmatched-node lifecycle traced); wire contract field-for-field across C#/d.ts/library (one documented visually-equivalent deviation: justification-left omitted); all 22 commits trailer-verified; suites re-run green at head. Minors triaged: 2 doc one-liners FIXED in wrap commit (README category-exclusion wording, schema.json URL-or-path description); rest accepted/folded to Phase 5 (list in review output + phase4-notes). New review minors (accepted, no action): demo-only fallback asymmetry (classification=simplified table vs validation=defaultStyleInfo); defensive Unknown-entry divergence tokenizer-vs-guards; pathological \+f note token; MarkerValidationPlugin styleInfo prop unmemoized upstream-dependent.
PHASE 4 COMPLETE pending user decisions: (1) popover delta-adaptor fix scope (pre-existing getNoteOps glyph-as-text defect; Save-corruption risk flagged); (2) push approval. Branch heads: scripture-editors standard-view (wrap commit next), paranext worktree 54bf9155fc8. NOT pushed.
Task 14 (USER-SANCTIONED popover delta-adaptor fix): approved 2026-07-04. USER CONTEXT: the OT collab path was never fully completed — bugs there are not surprising; fix the note-serialization contract without assuming the surrounding path is finished. Scope: canonical glyph-free note serialization (getNoteOps/$handleTextNodes), well-formed popover applyUpdate, clean popover-Save writeback, FootnoteEditor \-keydown gate for editable markerMode, in-app QA item 7 re-verification + regressions.
Task 14: DONE_WITH_CONCERNS (report: .superpowers/sdd/task-14-popover-report.md; NOT task-14-report.md — that name is Phase 2's). Library 2a09b69: canonical glyph-free note contents ops in editable markerMode — $handleTextNodes skips glyph MarkerNodes/editable caller text/structural NBSP inside notes; $getNoteOp carries unknownAttributes (closed="false" round-trips); $createNote re-adds NBSP separator + honors closed. Note contents ops now markerMode-invariant (opsGen1v1Editable/opsGen1v1Standard note contents = opsGen1v1's; only those two pinned expectations changed). TDD red->green incl. new note-ops-popover-roundtrip.test.tsx (2x Editorial, exact FootnoteEditor flow: ops fixed point, USJ deep-equal incl. \fv + unclosed, $rebuildNoteContent no-op fixed point, Save-leg cleanliness). Gates green --skip-nx-cache (tests/typecheck/lint x4 projects, whole-workspace tests, extract-api no drift). Worktree 9fc945fff82: FootnoteEditor \-keydown gate off when markerMode==="editable" (+dist). Propagated per runbook (probes x3 in main.js incl. the gate). In-app QA item 7 PASS: popover loads well-formed (single glyphs, 0 unmatched — state), Tier-1 \ft->\fq restyles, \zln->char blue, \zfoo->char status_unknown, literal \ reaches editor, Save writes CLEAN note content to state+SFM disk; regressions pass (\zfoo body split, s1 red, Ctrl+T); 0 app console errors; doc/disk restored via undo (verified). NEW PRE-EXISTING FINDING (out of scope, documented): popover-Save replace POSITION displaced +5 — $getOTPositionOfNode double-counts editable ChapterNode (embed+\c-glyph-text) vs $applyUpdate's opaque-embed traversal; owner decision needed (findings #2/#3 territory); also pre-existing popover wrapper-para \p-glyph junk (display-only) and mid-typing interleaving stray unmatched — all in phase4-notes limitations. NOT pushed.
Task 14: implemented (lib 2a09b69+0389b60, worktree 9fc945fff82), review APPROVED (contract mode-gated by MECHANISM — glyph skip requires NoteNode ancestor + MarkerNode presence; symmetric both ends incl. unclosed notes; 2-editor idempotence suite; blast radius exactly 2 sanctioned fixture blocks; trailers verified). CONTROLLER RATIFIED: mode-independent closed/unknownAttributes carry (closes Phase 3's recorded OT closed-threading gap). Fix round dispatched: sentinel Save-leg assertion (Important — current test can't detect silent no-op write), caller-skip positional guard, phase4-notes headline honesty. IN-APP QA item 7 PASS (popover editing works: rename/classify/validate, literal backslash, content-clean Save). REMAINING USER DECISION (findings #2/#3 owner call): popover Save POSITION displaced +5 in editable docs ($getOTPositionOfNode counts editable chapter glyph text; applyUpdate treats chapter opaque) — Save duplicates note +5 late, original not deleted, one char eaten; undo recovers; NOT usable until fixed. Repair options: (a) opaque-chapter counting host-side (correct locally; diverges from pinned delta-DOC coords) vs (b) apply side honors doc coordinates (wholesale editable apply change).
Task 14: COMPLETE (lib 2a09b69+0389b60+5179955, worktree 9fc945fff82; fix round controller-verified: tree clean, trailer exact, no scratch files in repos; round-trip 3/3 w/ sentinel-proven Save leg, editor-delta 30/2skip w/ caller-position pin red->green, platform 314/3skip, shared-react 1081/2skip, typecheck+lint green). Popover marker editing SHIPPED and QA-verified. PHASE 4 + TASK 14 DONE. Open: (1) OT-coordinate decision for popover-Save position displacement (options a/b in Task 14 ledger entry above); (2) push approval for both repos.
Task 15 (USER-DECIDED option 1): make $getOTPositionOfNode treat editable chapter contents as opaque (host-local popover-Save correctness; delta-DOC coordinate divergence accepted — collab incomplete). Then per user: push all updates (scripture-editors->fork, worktree->origin), full cleanups, emit Phase 5 starting prompt.
Task 15: DONE (report: .superpowers/sdd/task-15-report.md — overwrites Phase 2's stale same-named file). Call-site audit: 2 consumers pair with $applyUpdate (fix) vs 2 with getEditorDelta diffs (keep) → parameterized, not fixed-in-place. delta-common.utils.ts: new OTCoordinateSystem ("delta-doc" legacy default | "apply" = every embed opaque, matches $applyUpdate traversals; divergence doc on the type incl. pre-existing editable-VerseNode delta-doc wrinkle); $getOTPositionOfNode/$getNodeFromOTPosition/getInsertedNodeKey take it; $isOpaqueContentNode generalizes note/unknown opaqueness to element embeds (ChapterNode) in "apply" (general rule, not chapter special-case; decorator/TextNode embeds already consistent). $getReplaceEmbedOps retains "apply"; Editor.applyUpdate getInsertedNodeKey "apply"; DeltaOnChangePlugin fast path + handleChange getInsertedNodeKey stay "delta-doc" (pair with doc diffs; Ctrl+T popover-auto-open depends on it — host openFootnoteEditorOnNewNote). TDD red->green: 3 new delta-common tests (apply position 7 vs legacy 12 pin; reverse round trip; $getReplaceEmbedOps+$applyUpdate replace-in-place w/ getInsertedNodeKey) + Save-leg position assertions in note-ops-popover-roundtrip (count unchanged, sentinel at note 0, whole-doc deep-equal w/ only note 0 swapped). Gates --skip-nx-cache: shared-react 1084 pass, platform 314 pass, typecheck/lint (3 pre-existing warnings), format, extract-api platform no drift (shared-react has no api report). Propagated per runbook (delta-doc marker x3/x3/x9 hops); in-app wgPIDGIN GEN 1: Save in place through 7 live per-keystroke replaceEmbedUpdates (17 notes unchanged, sentinel only in target \ft, neighbors intact, SFM line 10 clean+positioned), undo restores state byte-identical + disk; regressions s1 red, \zfoo split+status_unknown (insertText methodology — app MarkerMenu intercepts raw '\' keys), Ctrl+T insert+popover auto-open (preserved delta-doc path) all PASS; 0 app errors (1 benign ResizeObserver). Cleanups: doc restored (17 notes, as-found paths, para8 byte-equal, 0 remnants on disk), Task-13 test custom.sty hg-reverted to original 858-line project file (copy in report), Settings.xml LEFT (diffs are user PT9 data — RepeatableWords/ModelTexts/resources; QA never touched it per Task 13/14 reports; documented), app npm-stopped, CDP down, no lingering xvfb/electron/pw-server. NOT pushed.
Task 15: COMPLETE (001d997..70cc67e = 5dc6c01 fix + 70cc67e docs, review APPROVED — all 7 named risks verified vs source; audit complete: 2 consumers -> "apply" coords, 2 kept legacy delta-doc (Ctrl+T auto-open preserved, verified live); default path provably byte-equivalent; forward/reverse symmetric via shared $isOpaqueContentNode; Save-leg whole-doc deep-equal forbids displacement; in-app 7 live per-keystroke in-place replaces, cleanups done incl. custom.sty hg-revert). Report's "apply-side descends into UnknownNode" claim corrected post-review (phantom — $isEmbedNode includes UnknownNode; EmbedNode type-union omission is the real cleanup candidate). POPOVER SAVE FULLY USABLE. Phase 4 + Tasks 14/15 all complete; pushing per user order.

# SDD progress ledger — Standard view Phase 5 plan (docs/superpowers/plans/2026-07-04-standard-view-phase-5.md, plan commit ba8eaed, spec 1f7f74c)
Part A = scripture-editors standard-view (Tasks 1-8); Part B = paranext worktree standard-view (Tasks 9-15); Task 16 wrap-up. Base before Task 1: ba8eaed.
Task 1: COMPLETE (report: .superpowers/sdd/phase5-task-1-report.md). PT9 MarkerItemSource port — new packages/platform/src/editor/markerMenu/markerItemSource.ts (getMarkerMenuItems/getEnterMenuItems + MarkerMenuContext/MarkerMenuItem), exported from the platform barrel; markerValidation.utils.ts's isParagraphTagValid/ParaStackEntry exported (no behavior change, verified by its unchanged 19/19 suite). Two brief-text corrections made and documented in the report: (1) close-tag `+`-prefix assignment — the brief's step-2 example text had it backwards relative to its own rule-3 formula and the phase5 design doc (`:121-122`); implemented per the formula/design (inner span gets `+`, outermost doesn't). (2) the `previousParaMarkers: ["p"]` paragraph-source test is mechanically unsatisfiable as literally written — the shared fixture's p/q1/q2/s1 all declare `occursUnder: ["c"]` but the brief's fixture list omits a `c` entry, and a stack replay of just `["p"]` can never contain "c" (TagValidator only bypasses occursUnder when the stack is fully empty); added `c` to the fixture and used `previousParaMarkers: ["c", "p"]`, which reproduces exactly the brief's expected offered set. Also found the brief's suggested `q10`-in-fixture ordering check is permanently unreachable through the public API (no real USFM marker in the ParaNode/CharNode/NoteNode allowlists has a 2+ digit suffix, and `isUsjMarkerSupported` hard-gates candidates independent of styleInfo content) — verified the digit-aware tie-break via a directly-exported (module-internal, not barrel-exported) `compareMarkerText` instead. TDD: RED (module missing) -> GREEN (11/11 new tests); full project suite 325 passed | 3 skipped (pre-existing skips, unrelated); lint/typecheck/format clean; extract-api committed (28-line api.md addition, only the 2 pre-existing unrelated tsdoc warnings remain).
Task 1: complete (commits ba8eaed..1ec8cee, review APPROVED by opus; 11/11 new + platform 325/3skip, gates clean). THREE brief deviations all adjudicated implementer-RIGHT vs PT9 source (close-tag + direction: +nd*/wj* for ["nd","wj"]; fixture needed c for occursUnder replay; q10 unreachable -> compareMarkerText direct test).
  Minor (recorded): close-tags-first is a spec-mandated divergence from PT9's final OrderBy(IsBasic) which would float basic char styles ABOVE close tags — ESCALATED to user (answer pending; 5-line fix either way); getEnterMenuItems recomputes buildParaStack (trivial); char->para fallback unconditional (selection-aware gating deferred to Task 3 wiring per brief); compareMarkerText exported module-level for test only.
Task 2: complete (commits 1ec8cee..b42a4a4 = ece0f2c impl + b42a4a4 fix, re-review APPROVED; 9/9 focused, platform 334/3skip, gates clean). Fix round: $isAtParagraphContentStart was non-recursive -> misclassified caret at visible start of a leading char span (\p \wj... red-letter shape) as character source vs PT9's structure-agnostic probe; fixed via $getFirstLeaf descent unifying both branches; RED-recorded + pre-fix-GREEN boundary guard (past-glyph stays character).
  Minor (recorded): no wiring-level test for Editor.tsx isReadonly guard; documented asymmetry — any offset inside PARA prefix counts as content start but only offset 0 of a char-span opener glyph does (deliberate, pinned by test b).
Task 3: complete (commits b42a4a4..dbd4ab5, review APPROVED by opus; 6/6 new, platform 340/3skip, gates clean). Beyond-brief $wrapNode fix in usj-marker-action.utils.ts adjudicated NECESSARY-FOR-SPEC (editable-mode wrap discarded glyphs -> transform unwrapped; same class as Phase-3 cc82802) — all 3 named risks verified: non-editable modes fall to byte-identical else branch (visible mode uses ImmutableTypedTextNode, disjoint from MarkerNode); 26 existing tests cover unchanged branch. Single-undo pinned by real UNDO_COMMAND round trips.
  Minor (recorded): $selectAfterCharNode guards $isTextNode without !$isMarkerNode (caret can land in a glyph on content-end close of inner span; narrow, untested); content-end closeTag branch + nested-outer degrade fallback + note-kind apply + raced-prefix untested; split caret asserted in-update only (jsdom selectionchange rationale sound); scribe's parallel usj-marker-action copy untouched (out of scope — check at final review).
Task 4: complete (commits dbd4ab5..f28c943 = 14d64b9 impl + f28c943 fix, re-review APPROVED; 11/11 focused, platform 347/3skip, gates clean). Sanctioned fallback test route taken (jsdom has neither execCommand nor ClipboardEvent — verified); duck-check used in MarkerEditPlugin per brief's own fallback (instanceof throws in jsdom). Fix round (Important, plan-mandated — brief's verbatim code): event-shaped payload w/ null clipboardData wrongly took null-dispatch branch (re-entrant execCommand risk, no preventDefault); decline (return false) restored per spec's real-event-identical requirement, RED-verified test pins all 4 decline surfaces. Reviewer independently verified @lexical/clipboard@0.43 internals (data param bypasses CRITICAL interception; LexicalClipboardData exported).
  Minor (recorded): brief claimed an "existing" non-standard-view gate test that doesn't exist anywhere (brief-authorship error; gating code pre-existing/untouched — candidate cheap test at final review); decline path builds payload before declining (wasted read-only work, rare edge); decline test placed in null-leg describe block (cosmetic); report tally prose inconsistency (doc noise); Object.entries loses string typing on setData values (inert).
Task 4 TRUE END-TO-END clipboard verification deferred by design to Task 15 in-app QA (clipboard bytes).
Task 5: complete (commits f28c943..e7e0ec4 = 0983d6d impl + e7e0ec4 test fix, re-review APPROVED; harness 9/9, platform 356/3skip, shared-react 1084/2skip). QA-only editableHarness branch on UsjNodesMenuPlugin (structural types, no platform import; non-editable path untouched — early return); Enter interception CRITICAL above MarkerEditPlugin HIGH verified against Lexical dispatch internals; splitExpected never set when intercepting. Impl-before-tests TDD deviation disclosed; first test pass caught 4 real bugs (TextNode-merge staleness, cleanup misfire, JSX bare-attribute backslash doubling, weak substring assert); fix round pinned noteMarker/inMarkerText pass-throughs via direct INSERT_PARAGRAPH dispatch (the non-keyboard source class the guards defend) w/ positive control + mutation-RED (which itself caught an empty-menu assertion weakness). Reviewer: TDD deviation fully mitigated.
  Minor (recorded): defensive comment overstates keyboard-unreachability for noteMarker (exact only for inMarkerText; non-collapsed in-note Enter CAN reach the guard — behavior correct+pinned; one-line comment precision fix candidate); wrap-case test doesn't complete selection to assert literalPrefixLanded:false receipt; test (a) post-dispatch doc assertion loose (QA-only latitude); scoped jsdom Range polyfill (Phase-2 precedent).
Task 6: complete (commits e7e0ec4..75bdc0a = cb5defe impl + b5d4848 ref fix + 75bdc0a test fix, re-review APPROVED; shared 166/166, platform 356/3skip incl. 64-case corpus). §7 visible inert UnknownNode: class-based createDOM (unknown-block/unknown-inline + data-marker/data-tag + contentEditable=false), CSS default-hidden everywhere, revealed subdued under .marker-editable only; bare .status_* rules deleted (scoped survive, verified); data-tag emission closes a pre-existing $convertUnknownElement import asymmetry. VERIFY-FIRST FINDING + CONTROLLER ADJUDICATION (user informed, standing veto): \ref is corpus-proven mid-paragraph inline like \optbreak — spec §7's own rationale governs over its literal block-bucketing; INLINE_UNKNOWN_TAGS={optbreak,ref}; // label optbreak-only via [data-tag]; ref child text visible. Caret-adjacency pinned model-level after fix round (both boundary legs, anchor-ancestry + byte-identical subtree, mutation-RED).
  Minor (recorded): test locals use null over undefined (style nit, both test describes); stale "not exercisable here" wording in deletion-describe comment; collapse precondition of anchor-only ancestry checks implicit; bare-backslash label for markerless blocks (e.g. table) cosmetic; nested-borders cosmetic (row/cell in table); block deletion test pins pre-existing engine behavior (limited diagnostic value).
Task 7: complete (commits 75bdc0a..779d70f, review APPROVED clean; shared-react 1086/2skip, scribe 2 (first scribe test file), platform 356/3skip). (1) OnSelectionChangePlugin editor.read -> getEditorState().read (9b hazard class removed; reviewer verified mechanism line-for-line vs lexical@0.43.0 source: unconditional $commitPendingUpdates freeze -> getWritable setCachedNodes throw; staleness confined to already-hazardous nested case, self-heals on next native dispatch); regression test reproduces the literal crash standalone in shared-react, RED-verified. (2) Scribe CommandMenuPlugin gate (viewOptions was already a plain prop; gate test drives real getViewOptions mapping), RED-verified. (3) \xq quotation test (exact xq node text asserted; GREEN-first as expected). (4) CLAUDE.md Lexical 0.43.0.
  Minor (recorded): getEditorState().read omits the {editor} options bag editor.read passed — getActiveEditor() would be null inside the callback; inert today (verified whole call graph), latent footgun if selection.utils ever needs editor context; brief-verbatim. Triplicated jsdom getBoundingClientRect stub (existing convention).
Task 8: IN PROGRESS w/ findings (report .superpowers/sdd/phase5-task-8-report.md). Agent wedged twice on hung browser calls (clipboard.readText class); killed + salvaged. GATES ALL PASS at 779d70f (1632 tests/5skip across 9 projects, typecheck 10, lint 0err/43 pre-existing warn, format clean, no api drift). Demo QA: items 3,5,6,7 PASS (§7 blocks verified live; wrap path clean; Enter menu splits/cancels clean); item 2 partial (char list + literal-landing PASS; close-tag-first ordering not exercisable in fixture); TWO REAL DEFECTS: (A) item 1 — paragraph-kind apply from \-menu at para content start corrupts doc (target untouched, 2 bogus paras spliced elsewhere; PT9 semantics = RETAG current para, not insertParagraph branch); (B) item 4 — collapsed-cursor char insert corrupts word remainder into phantom marker node ("ked," marker; adaptor warning usj-editor.adaptor.ts:216); undo recovers both; wrap path unaffected. Items 8,9 + devpub + cleanup pending. Tooling note for future browser QA: browser_type without slowly:true = .fill() WIPES the Lexical doc; use slowly:true/press_key. Enter-menu default highlight in harness is context-sensitive (m/q2 observed), not p-first — harness-only latitude (P10 palette highlights index 0); recorded.
USER DECISION applied: close-tag ordering follows PT9 (final stable OrderBy(IsBasic) over combined char-source list; basic chars float above close tags) — fix dispatched to Task 1 implementer. Then: defects A+B fix -> Task 3 implementer; re-reviews; Task 8b agent finishes QA items 1,4,8,9 + devpub + cleanup. Dev server (nx dev platform, :5173) left running for 8b.
Task 8 fix wave: complete (779d70f..3e0c0b4 = 11bf500 ordering + 3e0c0b4 defects, review APPROVED by opus vs PT9 source; platform 361/3skip). 11bf500: PT9 final stable OrderBy(IsBasic) over combined char-source list (USER DECISION; exact-array pin incl. stability + innermost-first close tags). 3e0c0b4: (A) paragraph-kind menu apply = RETAG current para at content start (same $isAtParagraphContentStart probe as menu-source — decisions can't disagree) / $splitParagraphWithMarker mid-text; getUsjMarkerAction insertParagraph branch unreachable for real para markers ('c' keeps structural action). (B) ROOT CAUSE was Phase-2 blur resolution, not the insert path (no-blur control proved insert clean): menu click blurs editor -> BLUR handler Tier-2-resolved the pending literal -> phantom marker; FIX = blur excepts the caret's node via the pre-existing exceptKey mechanism (one-argument delta; Enter/departure still complete; PT9-vindicated: DisableReformat brackets Close(), popup interaction can never commit the typed run; also load-bearing for P10 palette clicks which blur the iframe).
  Minor (recorded; FOLLOW-UPS REGISTER items): abandonment-window divergence — blur-and-never-return leaves caret-node literal pending indefinitely; sharpest form: mid-rename glyph + host save serializes OLD marker while screen shows new (self-heals next caret commit); policy decision candidates: resolve-all on host save/getUsj, or menu-mousedown preventDefault. Enter-trigger branch of $applyParagraphSelection untested (low exposure). Type-through window (\ + existing text terminating literal on typing commit) open — same defect family, expect at host QA (Task 15).
Task 8 + 8b: COMPLETE (verification; no code commits; HEAD 3e0c0b4). Gates: whole-repo PASS at 779d70f (1632/5skip, 10-proj typecheck, 0 lint err, format clean, no api drift) + delta gates PASS at 3e0c0b4 (platform 361/3skip, typecheck/lint/format clean). Demo QA final: items 1,3,4,5,6,7,9 + ordering spot-check PASS (retag in place; remainder stays plain text; basic items above close tags; §7 blocks live; 0 console errors); item 2 partial (close-tag-context menu exercised post-fix via ordering check); item 8 clipboard NOT VERIFIABLE in sandbox (readText timeout + execCommand blocked) -> Task 15 in-app covers. Bridge: devpub pushed, worktree d.ts has all 5 new exports (getMarkerMenuContext/applyMarkerMenuSelection/splitParagraphWithMarker/getMarkerMenuItems/getEnterMenuItems). Yalc note: global push touches other consumers' gitignored node_modules symlinks incl. main checkout (inherent, no tracked content). Cleanup verified (server killed, port free, tree clean). Browser-QA tooling notes recorded in report (browser_type fill() wipe; hang classes). PART A COMPLETE: Tasks 1-8 done at 3e0c0b4, all reviews approved.
FOLLOW-UPS (Task 9):
- Passive palette: host filters raw labels vs component renders localized — LocalizeKey labels diverge; constrained via JSDoc to plain-string labels; localization-aware filtering = follow-up.
Task 9: complete (worktree 9fc945fff82..bd0eb2c5e3e = ac2cabdee36 impl + bd0eb2c5e3e fixes, re-review APPROVED; overlays scope 200/200, tsc/eslint clean, papi.d.ts regen committed both times). Passive palette: flag on CommandPaletteRequest + webViewId-keyed drivers (update/commit/dismiss) + store clamping mutator + passive component mode (no input, no focus steal — cmdk CommandEmpty/List provably non-reusable: filtered.count needs registered items; List clobbers caller aria-activedescendant). papi exposure = IOverlayService extension only (JSDOC DESTINATION markers). Fix round: ordinal (PT9) filter matching + corrected JSDoc; LocalizeKey containment as dual @remarks + FOLLOW-UPS ledger line (passive palettes require plain-string labels; localization-aware filtering = follow-up); role=option + ids + aria-activedescendant on passive list.
  Minor (recorded): passive listbox lacks accessible name (cmdk default "Suggestions"); PT9 dual-StartsWith residual — literal "+w"-labeled item matches filter "+w" in PT9 but not strip-only port (conformant to brief; durable note here for Tasks 10/11 close-tag items); disabled-skip commit uses wraparound (defensible reading, implementer-flagged).
Task 9 ADDENDUM in flight: defaultStyleInfo platform-barrel export + re-devpub (haiku bridge agent) — Task 10/11 dependency discovered by controller pre-check (0 hits in linked dist).
Task 10: complete (worktree bd0eb2c5e3e..2d34b988cf7 = e4e51abe6ca impl + 2d34b988cf7 fixes, re-review APPROVED; extension 202/202 (+7), renderer overlays 203/203 (+3), build:extensions clean, root tsc clean, lint delta-clean, papi.d.ts regen). Standard-view \/Enter palettes: forwarding table verbatim (Space/* precedence before mirror regex — * dismisses; [a-z0-9+] mirror), legacy \ branch byte-preserved w/ viewType guard, Enter passes through on !ctx/noteMarker/inMarkerText, empty items -> no palette, ABORTED as dismissal; generateInlineMarkerMenuListItems re-sourced to library API (structure protection kept, legacy sort dropped); real TS7053 LocalizeKey-indexing bug caught by webpack and fixed w/ isLocalizeKey guard. Fix round: badge 'end' + NEW CommandPaletteItem.muted (opacity on text block, both modes, provably inert to selection) — group deliberately NOT used (would break PT9 interleaved order; pinned by group===undefined test); session-token guard on all 4 async clears (pure clearPaletteSessionIfCurrent helper, 3 pins incl. the dismiss/re-trigger race); commit clears session synchronously; pure modifier keydowns ignored in-session.
  Minor (recorded): commit-on-empty-filter orphans an open palette until click-outside (candidate: dismiss when filtered list empty, or host resolve-undefined on no candidates — Task 15 runtime will show if it matters); AltGraph/CapsLock still dismiss (AltGr-chord layouts, same shape as Shift bug, far rarer); web-view keydown block has no runtime harness (Task 15 covers).
Task 11: complete (worktree 2d34b988cf7..48db2e9a50a, review APPROVED clean; lib 237/237 (+15, RED via stash: 11-fail/4-pass split independently verified by test structure), extension 202/202, builds/typecheck/lint clean, dist rebuilt+committed ~6100-line pure bundler churn). FootnoteEditor markerPalette prop family (local PaletteItemLike, structurally CommandPaletteItem-compatible incl. badge/muted); forwarding table byte-for-byte Task 10 parity (only driver target differs); local token-guard + mapping duplicates (plan-mandated, no cross-imports); Enter-in-popover untouched (\fp); degradation paths preserved (no prop = pass-through; non-editable = legacy MarkerMenu byte-identical under else); web view single useMemo impl passed at render; anchors iframe-relative pass-through.
  Minor (recorded; FOLLOW-UPS candidates): ~50-line forwarding table + helpers now duplicated verbatim across web view and FootnoteEditor (plan-mandated; drift risk; candidate future shared hook in platform-bible-react); main web-view keydown captures .editor-input once per effect (double-handling safety rests on portal-ordering + dep-array timing — pre-existing Task 10 shape, safe by construction); report's "experimental unchanged" phrasing covered only the type surface (16-line cjs/js chunk churn).
Task 12: complete (worktree 48db2e9a50a..07679e89c17 = 45c2a7cade0 impl + 07679e89c17 docs fix, review APPROVED; dotnet build clean, c-sharp 1317/6skip, new NoteCallerAndSeparatorSettingTests 12/12, csharpier/typecheck/build:extensions/lint delta-clean). Four settings mapped (name-map ONLY — zero ParatextProjectDataProvider changes; generic ParametersDictionary fall-through + GetDefault RPC verified end-to-end incl. JS-side projectSettings.json default resolution); web-view useProjectSetting reads w/ old fallbacks as hook defaults; two-layer default semantics documented (registered "." for chapterVerseSeparator is a REAL PT9-faithful behavior change for tag-less projects vs the old hard-coded ":"). CALLER-SEQUENCE OUTCOME: no sequence settings exist in ParatextData (PA/PublishingAssistant's CallerSequence is a different subsystem/prefs store) — noteCallers stays library default, crossRefCallers ['†'] stays hard-coded (spec-sanctioned). Fix round (plan-mandated overclaim in MY brief table): "+"/"-" caller defaults are the PLATFORM'S choice (conventional usage + prior fallbacks); ParatextData's own GetSetting fallback for them is "" — provenance corrected in comments/tests.
  Minor (recorded): four near-identical hook/guard/memo blocks (~60 lines; matches file precedent; helper if pattern grows); new setting labels/descriptions authored fresh (no PT9 UI copy found) — copy pass before release if surfaced in UI.
Task 13: complete (worktree 07679e89c17..96bc70cbb9c = 85586d3d07b impl + 96bc70cbb9c fix, re-review APPROVED; typecheck/build:extensions/lint delta-clean). Power default: verify-first step PAID — naive isPowerMode?-default is STICKY-wrong (useSetting isLoading unconditionally true first render + useWebViewState lazy init captures permanently); shipped = default expression + one-shot ref-guarded correction effect with raw-store discriminator (globalThis.getWebViewState probe; reads never persist; VIEW_TYPE_UNSET sentinel). Fix round (Critical): mount-time snapshot could clobber a user's mid-flight cycle choice — now re-probes the store AT FIRE TIME as the decider (persist path and probe verified synchronous against the same dock-layout store — no interleaving window; fast-path snapshot is no-write-only); ref set on bail path too; correction write intentionally sticky (documented). Task 15 QA addendum: (1) fresh power view -> immediately cycle to markers -> resolution -> STAYS markers; (2) post-cycle interfaceMode re-toggle no re-fire; (3) fresh power view -> standard; saved views keep type; simple unchanged.
  Minor (recorded): sentinel doc comment stale (cites mount probe as the use; fire-time decider is the real one — cosmetic).
Task 14: complete (worktree 96bc70cbb9c..62373b3a314, review APPROVED by opus; lib 239/239, gates clean, dist rebuilt). Part 2: wrapper glyph FIXED via candidate 1 — EDITABLE_WRAPPER_PARA_PREFIX_RETAIN=3 ({retain:3} before noteOp; \p glyph 2 + NBSP 1; reviewer independently verified vs apply-side traversals which count MarkerNode as full-length text; editable-only via ref, no effect retrigger; non-editable byte-unchanged; ops contract untouched; RED reproduced exact 5-child artifact). Part 1 AUDIT LANDMARK: named paths clean, but "Cancel leaves fresh note" is REAL + LIVE — pre-existing delta-doc skew: getEditorDelta emits BOTH text op AND verse embed op for an editable VerseNode while delta-doc reverse lookup counts text only -> getInsertedNodeKey lands +1-per-preceding-verse PAST the note -> replaceEmbedUpdate(wrongKey) hits non-embed -> $getReplaceEmbedOps undefined -> silent no-op. RECONCILIATION (reviewer, resolution a): fresh-note key comes from the skewed delta-doc getInsertedNodeKey; EXISTING-note key comes from noteCallerOnClick's direct Lexical key — which is why Phase-4/Task-15 Save-in-place QA passed while ITS OWN report separately recorded "Popover Cancel closed without deleting" (task-15-report.md:150-154). Scope "essentially always for NEW notes" correct (verses are the only delta-doc-internal skew source).
Task 14b DISPATCHED (controller decision, plan-intent "fix the residual"; OT coordinates untouched by design): candidate 2 — EditorRef.insertMarker returns the inserted NOTE's node key (captured in getUsjMarkerAction's note branch); host overwrites editingNoteKey.current with the TRUE key after insertMarker returns (popover already opened with correct ops by the unchanged auto-open path). Library + devpub + worktree.
  Minor (recorded): report doesn't explicitly tie the path split back to Task-15 Save QA (inference required); retain=3 magic constant tied to PARAGRAPH_USJ \p default (documented + test-pinned); Part 1 deliverable documentation-only (correct given repo scoping).
Task 14b: complete (lib ee4683e..7062b7e + worktree 62373b3a314..9a02adb44fa + 24fe6156b1e guard, review APPROVED by opus; platform 363/3skip, extension 202/202, gates clean, api.md 1-line drift). insertMarker returns the inserted note's TRUE Lexical key (closure capture in getUsjMarkerAction note branch, getInsertedNoteKey seam, RED-verified vs $getNodeByKey under full harness); host defers the editingNoteKey overwrite via queueMicrotask — DEVIATION from dispatch (sync assumption FALSIFIED: Lexical defers listener flush via scheduleMicroTask) — reviewer PROVED ordering by construction (scheduleMicroTask IS queueMicrotask, :11083; zero async hops in the M1 chain; FIFO); failure containment verified (inversion = old no-op, mechanism can never delete); wedge guard added (controller-verified 5-line diff: only correct a key the auto-open actually set). OT coordinate code untouched in both repos (hard constraint held).
  FOLLOW-UPS (Task 14b): double-insert retarget race — two rapid Ctrl+T during the pre-insert await window can retarget editingNoteKey to note B while popover edits note A -> Cancel deletes B (pre-fix: left both; post-fix: deletes wrong new note; needs per-insert session token tying the correction to its popover session — exceeds envelope, recorded). Underlying delta-doc verse-skew in getInsertedNodeKey remains (bypassed for this flow only; unification = collab work). Lexical-upgrade dependency: ordering proof tied to scheduleMicroTask==queueMicrotask in 0.43.
Task 14 + 14b CLOSED. ALL CODE TASKS (1-14b) COMPLETE. Worktree HEAD 24fe6156b1e; library HEAD 7062b7e.
Task 15 FIRST PASS: DONE_WITH_CONCERNS (report phase5-task-15-report.md, written incrementally). Gates ALL PASS both repos (extension typecheck/lint delta-clean; dotnet 0 err; C# 1317/6skip; library whole-repo at baseline: shared 166, shared-react 1086/2skip, platform 363/3skip, scribe 2). Propagation build-time hops verified + agent found and rebuilt a STALE COMMITTED lib/platform-bible-react/dist (left uncommitted for fixer to verify+commit). BLOCKER FOUND (real, pre-existing since Tasks 9-11 landed, first caught by the phase's FIRST genuinely cold npm-stop->rebuild->relaunch — prior 6 QA sessions never re-ran activate()): platformScriptureEditor FAILS TO ACTIVATE — react/jsx-runtime unreachable in extension-host sandbox; Task 10's getMarkerMenuItems/defaultStyleInfo VALUE imports in platform-scripture-editor.utils.ts (also imported by main.ts) drag the editor package's React-bundled monolithic dist into main.js. 3 local workarounds tried+reverted by QA agent. QA items 1-10,12 NOT VERIFIABLE this pass; item 11 partial (wgPIDGIN Settings.xml: ':' and '-', no caller override — so project values happen to equal old fallbacks). Cleanup verified. FIX DISPATCHED to Task 10 implementer: split editor-package value imports into a web-view-only utils module (main-bundle graph editor-free), bundle-level assertion, COLD-START ACTIVATION SMOKE (new permanent QA requirement), verify+commit the dist rebuild, scratch sweep. QA re-run follows the fix.
Task 15 QA RUN 2 (post-activation-fix, HEAD 909556b033f; report appended): DONE_WITH_CONCERNS. LIVE-VERIFIED WINS: item 7 partial-PASS (Ctrl+T insert+auto-open; 14b Cancel removes fresh note BYTE-CLEAN; Save-in-place no-dup; NO wrapper-glyph junk — Tasks 14/14b live); item 8 PASS (real clipboard bytes NBSP-clean copy+cut; USFM paste Tier-2); item 9 PASS a/c (fresh power view=standard; saved kept; b timing not reproducible; d partial); item 11 PASS DISCRIMINATING (papi chapterVerseSeparator ':' != registered '.' proves Settings.xml read path; \fr 1:9); item 12 mostly (project CSS standard-only, s1, cycle doc-byte-equal, toggle, 0 errors, latency). FAILURES (3 clusters, real-browser event/focus semantics jsdom can't see): (A) items 1-4+zfoo — literal \ FORCE-SETTLED ~190ms after landing (instant para split, caret ejected, AUTOSAVED \p \, NOT undoable; timeline captured) — hypothesis: renderer overlay focus shift -> real iframe blur NULLS Lexical selection -> blur resolve-all (Task-3 except-guard has no anchor); load-bearing fix = except lastAnchorKey (already tracked) instead of live selection; (B) item 6 — Enter splits BEFORE choice (Lexical handles Enter on root keydown synchronously; window BUBBLE preventDefault too late -> need CAPTURE phase or in-editor interception); Escape no-cancel; highlight ip-not-p; POPOVER Enter=plain split no \fp (regression, investigate); item 5 — selection gate absent: \ REPLACED selection (text loss; investigate why branch not taken); in-popover \ palette didn't open (investigate); caller-click popover open FAIL (pre-existing Phase-4 open observation, now confirmed); (C) item 10 — §7 CSS never ported to the extension's FORKED _usj-nodes.scss (GEN 6: 40 UnknownNodes intact but invisible all views; plan authorship gap — library-only port). Cleanup verified byte-clean. FIX WAVE dispatched (opus): clusters A+B+popover regressions (library engine + extension capture-phase rework) + mechanical scss port (C).
Task 15 FIX WAVE round 2 (+outage recovery): library 7062b7e..0412767 (823f385 A-gate, 5f1e8d1 B-ip, 5e80b84 A-hardened, bc8f934 self-echo suppression, a9b8cdb test fix, cf3da4e external-USJ-replace must not touch unfocused editor's DOM selection, 0412767 BookNode cursor mover skips same-doc reloads = FALSIFICATION 2 — the true yank/focus-steal actor); worktree 909556b033f..5afaa4a292 (3a84ca8936a B-capture+E-css, 52832c6514 own-write PDP echo guard, 8321aea365 flag-free live-content deferral, 5afaa4a292 700ms DEBOUNCED keystroke PDP saves = root fix killing the echo storm; new use-editor-pdp-sync.hook.ts). SMOKE (cold, final HEADs, propagation re-verified after a FOREIGN 07-06 yalc push had overwritten the linked dist — re-devpub'd, fingerprinted): activation clean; TYPE-THROUGH FLUENT (headline deliverable LIVE); Enter no-premature-split + palette; popover \ palette opens focus-retained; §7 CSS live; popover Enter->\fp FAIL (DOM caret at wrapper start; onOpenAutoFocus-preventDefault candidate FALSIFIED live — Radix autofocus is load-bearing for into-popover handoff; follow-up: re-assert selectNote(0)+focus post-settle). Dangling web-view diff REVERTED (was the falsified candidate); strays cleaned. Gates: library whole-repo green (shared-react 1088/2skip, platform 372/3skip incl 6 new, scribe 2); extension tsc 0/lint delta/204/build clean. MAT probe damage hg-reverted; GEN/JHN/ACT pre-existing state documented.
ELEVEN fix-wave commits UNREVIEWED -> wave review dispatched (opus, both repos) incl. named adjudications: 700ms save-debounce data-safety (flush on blur/close/crash?), suppression-window semantics, echo-dedupe correctness vs legitimate same-ref navigation, BookNode mover skip correctness, cf3da4e blast radius (all external replaces!). QA RUN 3 (remaining items 1,3,4,5,6-full,12-zfoo) after review.
FIX WAVE REVIEW (opus, both repos): engine side APPROVED across all 9 adjudications — suppression windows terminate (mouse-only persistence benign, now documented ab1445f); FIFO self-echo dedupe clobber-proof w/ correct consume-staler semantics; BookNode book|chapter identity skip correct (only miss: project-switch-to-same-ref, guarded by mover range checks); cf3da4e blast radius verified benign for scribe (null-selection loads; AutoFocusPlugin places carets) and popover (selectNote is post-focus explicit); capture-phase scoped correctly (guards before any preventDefault, stopPropagation not immediate, legacy path untouched); \ip intro gate matches PT9 for genuine intro positions (sheet-rank-independent simplification); iteration layers coherent (no dead flags; debounce kills storm at source, hook deferral defends resumed typing); 6 new tests mechanism-pinning RED-worthy. ONE Important: debounce lacked flush/cancel lifecycle -> FIXED 3c74f87585 (flushable-debouncer.util.ts schedule/flush/cancel; flush in effect cleanup keyed on book/chapter BEFORE saver re-points — stale-write killed; unmount/dispose same path; best-effort blur/pagehide/beforeunload flush; 5 fake-timer pins; extension 209 green). Minors: 700ms "PT9-parity" comment softened (no cited PT9 constant; correction note appended to report); appPlacedCaret residual documented. LEDGER REPAIR: an agent's stash dance left AA-conflict markers in progress.md (empty upstream side vs full controller ledger) — markers removed, both sides preserved (dup Task-1 entry line 139 retained, harmless).
FIX WAVE + LIFECYCLE FIX: APPROVED (confirmation pass; flush-on-cleanup ordering verified real through BOTH ref layers — React runs all passive cleanups before any setups per commit; flush fires captured usj bypassing editorRef; debouncer contract pinned by 5 discriminating fake-timer tests; unload listeners symmetric; 204->209 reconciled). New Minor (watch in QA run 3): flush-on-window-blur fires a mid-marker save on the cross-frame palette-CLICK path (bounded, pre-wave-class exposure; trace-verified only — landed after the live smoke). Library HEAD ab1445f; worktree HEAD 3c74f87585.
Task 15 QA RUN 3 (HEADs ab1445f/3c74f87585, propagation md5-verified): DONE_WITH_CONCERNS. WINS: race classes DEAD (no premature settle while typing); retag-in-place via arrows+Enter AND via CLICK through the blur-flush (reviewers' watch item — clean, no doubling, SFM clean); Escape-leaves-literal + type-on; selection no longer destroyed by \; wj wrap via click clean; close-tag 'end' badge in correct PT9 slot; item-6 p-highlighted-first (ip fix live) + Enter-Enter clean split single-undo; \* literal leg; Ctrl+T->Cancel regression guard (17->18->17 byte-equal). REMAINING DEFECTS (4 clusters): (1) TEXT LOSS — mid-word fluent \wj merges remainder into phantom \wjke glyph + split; Enter-apply self-repairs structure but EATS "ke" (3 undos restore) — the recorded type-through-window family, now confirmed; (2) FOCUSED palette keyboard dead in-app (input never receives focus cross-frame: typing replaces doc selection, arrows collapse it, input-click closes palette — mouse-only usable) AND item-5-Enter-menu Escape COMMITS the split + zombie palette + REAL main-frame removeChild NotFoundError (overlay churn) — likely one cross-frame focus/teardown cluster; (3) departure-settle NEVER fires in-app (raw literals persist + serialize; hypothesis: PDP echo replaces re-arm appPlacedCaret continuously — every CURSOR_CHANGE-tagged commit re-opens the window, mouse-only departure never clears it); (4) muted (non-basic dimming) cosmetics absent in-app (tw:opacity-60 not taking effect?). Minor deviations: item-1 retag undo = 2 steps in-app (jsdom pinned 1); transient double-space in state (disk clean). Cleanup verified. FIX WAVE ROUND 3 dispatched.
Task 15 FIX WAVE ROUND 3: DONE (library 8f6c7a6 caret-bounded termination + trailing-space NBSP settling + window narrowing/click-clear; worktree 5b34745c55 focus-retry/input-click-exempt/enter-forwarding/muted-inline -> 175cc8bbdb selection-session forwarded driving + controlled active input -> 65a192ccc9 filterText-on-active). Cluster 1 (text loss): engine, not apply — remainder's own following space terminated the typed run; termination now caret-bounded; smoke fluent mid-word \wj intact. Cluster 2: focus fight unwinnable single-shot (Lexical re-focuses root every commit) — rAF retry + overlay-exempt dismiss + forwarding for enter AND new selection sessions + controlled input consuming forwarded filterText; smoke type-to-filter in box, Escape cancels no-split no-zombie. Cluster 3 (partial falsification): dominant cause = marker-trailing-space NBSP node EXEMPT from Tier-2 trigger + rebuild dropped its text; both fixed + narrowing (arm on real moves) + click-clears; smoke \zz settles on mouse departure. Cluster 4: tw:opacity-60 had NO backing rule in renderer (no tailwind build there) — inline style; smoke 32/38 dimmed. removeChild error didn't reproduce; 2-step undo = typed-literal + apply inherently two actions (follow-up doc). Gates: library 376/3skip + whole-repo + devpub; worktree 209 + overlays 184 + builds clean. UNREVIEWED — round-3 review dispatched (opus): named adjudications incl. 65a192ccc9 REVERSING Task 9's passive-only filterText rule, caret-bounded termination vs Phase-2 contract, NBSP-exemption change vs pinned behaviors, active-mode bit-identity for other consumers (hello-rock3), rAF retry bounds.
FIX WAVE ROUND 3 REVIEW: APPROVED (opus; zero Critical/Important). All 7 adjudications held: caret-bounding lives only in the typing-time trigger — settle path untouched, Phase-2 contract survives (paste caret sits after content; fixed-point keys on full text; test 76 body kept byte-identical, comment-only rewrite = least-destructive supersession record); NBSP narrowing safe (pure-NBSP prefix never reaches trigger — all Phase-2/3 fixtures byte-unaffected; signature/fragment share the conditional helper); narrowing arms off lastCommitAnchorKey updated on EVERY commit incl. suppressed (never stale; same-node tagged moves protected by except-anchor anyway); Task-9 filterText reversal RIGHT (hello-rock3 bit-identical — never calls updateCommandPalette; controlled input inits '' and self-filters); selection-session every-key claiming = PT9 wrap contract; rAF retry capped 20 + cancelled; muted inline coherent.
  Minor (recorded; FOLLOW-UPS candidates): mixed-modality filter drift (locally typed palette chars unknown to session.filter; next forwarded key resets input to stale string — document or sync-on-commit); Ctrl/Cmd chords swallowed as filter chars during selection sessions (copy unavailable while wrap palette open — branch claims before checking ctrlKey/metaKey); test 76 name stale + post-split glyph-typing path no longer mid-flow covered.
QA RUN 4 (final sweep) dispatched.
Task 15 QA RUN 4 (FINAL SWEEP, HEADs 8f6c7a6/65a192ccc9, md5-verified): ALL FOUR ROUND-3 CLUSTERS DEAD IN-APP (mid-word \wj intact through apply; mouse-departure settle live w/ status_unknown; selection-wrap fully keyboard incl. Escape-cancel clean; muted dimming + end badge + PT9 order). CONSOLE CLEAN both frames (removeChild did NOT reproduce). Regression spot-checks: Escape-literal, Ctrl+T->Cancel, clipboard NBSP-clean, power default, §7 CSS shipped (block visible standard/hidden formatted) all PASS. RESIDUALS: (1) Enter-menu type-to-filter not wired (char lands in doc + closes palette — enter-session forwarding lacks filter branch; bounded fix); (2) type-through \q1␣ settle SPLITS (empty-s1 orphan preserved) vs palette retag — CONTROLLER ADJUDICATION: document, don't fix — both PT9-faithful for their flows (PT9 popup REPLACES the typed run and retags; raw typed marker under reformat starts a new para, original kept — byte-faithful); PO-doc entry + followups; (3) empty-\fig unknown-blocks render 0-height (cosmetic CSS min-height); (4) NEW SUSPECTED REGRESSION: BookChapter chapter selection won't navigate (worked run 2; suspect round-3 controlled-CommandInput collateral in a shared component) — MUST TRIAGE before close. Undo-step counts documented (apply=2, settle=3 — typed-literal + engine actions inherently multi-entry; follow-up). Final bounded round dispatched: triage #4, fix #1+#3, targeted smoke.
Task 15 FINAL ROUND: DONE (worktree da1c8e9e7f nav fix + enter-filter + scss min-height; library 1e9f46d css mirror, devpub'd). NAVIGATION REGRESSION WAS OURS (round-2 latent, NOT round-3 cmdk): useEditorPdpSync's actively-editing deferral swallowed DIFFERENT-document arrivals (+ latent stale-save of old chapter through new chapter's selector); temporal fit exact (run 2 predated deferral; run 3 unfocused; run 4 focused). Fix = book|chapter identity gate on the deferral; unit pin; live-verified incl. focused-at-arrival. Cell-click chain undrivable via synthetic CDP (QA's own flaky note) — referred to next HUMAN QA pass, pipeline verified healthy. Enter-menu type-to-filter wired (smoked: q in palette input, doc byte-identical); empty \fig min-height both stylesheets (24/32px live). Gates: extension 210 (+nav pin) tsc/lint/build clean; library 376/3skip devpub'd. Bounded confirm review dispatched, then Task 15 CLOSES.
FINAL ROUND CONFIRMED (opus): identity gate correct both sides (arriving USJ's own book|chapter vs LIVE editor USJ — immune to prop timing; chapter docs always carry \c); stale-save leg closed and COMPOSES with lifecycle flush (cleanup flushes through OLD saver before setups re-point; mismatch now replaces without save-back); same-doc deferral verbatim intact (210 = 209 + 1 RED-worthy pin incl. the latent-leg discriminator); enter-filter pattern-consistent (deliberate divergences sound: per-key claiming for collapsed caret, [a-z0-9] for para markers; inherits Ctrl-chord wart); min-height floor-only. NEW Minor residual (FOLLOW-UPS): post-cleanup pre-arrival keystroke window — one debounced save can route through the new chapter's selector (sub-second, doubly narrowed by currentlyWritingUsjToPdp + arrival replace; the only remaining cross-chapter path).
TASK 15 CLOSED. Final HEADs: library 1e9f46d, worktree da1c8e9e7f. Phase 5 code COMPLETE and in-app verified: fluent type-through, palettes (passive+focused), settle-on-departure, wrap, Enter menu w/ filter, clipboard, power default, settings, §7 blocks, popover Cancel/Save/wrapper — all LIVE. Open in-app follow-ups: popover Enter->\fp; caller-click popover open; cell-click nav chain (human QA); + the recorded register.
Task 16: complete (docs-only wrap-up; no code commit range). Wrote the consolidated all-phases follow-up register docs/superpowers/specs/2026-07-07-standard-view-followups.md (single post-project handoff — supersedes spec §10 groups A-G; §10 items closed by Phase 5 marked with their closing task/commit, every Phase-5 "Minor (recorded)"/"FOLLOW-UPS" line swept in, grouped engine/palette-UX/popover/host-extension/styling/process-debt/DOCUMENTED-BEHAVIOR/parent-§13); Phase 5 completion notes docs/superpowers/specs/2026-07-07-standard-view-phase5-notes.md (phase-2/3/4 format: per-task commit ranges, the 4-QA-run + 3+final fix-wave saga told honestly incl. the falsification chains, handoff pointers); finalized the two communication docs against SHIPPED behavior — pt9-ux-differences.md (type-through/Space-commit now PT9-parity LIVE; added type-through-split-vs-palette-retag pair, popover Enter->\fp broken, caller-click unreliable, ~0.7s debounced-save crash-window, muted/badge affordances) and simple-mode-impact.md (marker-menu re-source + settings sourcing shipped; chapterVerseSeparator registered default '.' is a real tag-less-project change in ALL views; ~0.7s debounced-save reaches simple mode too; view-cycle decision still the team's). Report: .superpowers/sdd/phase5-task-16-report.md.
PHASE 5 COMPLETE. Standard View project (Phases 0-5) DONE. Final HEADs: library scripture-editors standard-view 1e9f46d; extension paranext-core worktree standard-view da1c8e9e7f. All user-visible Phase 5 behavior in-app verified. Post-project work: see docs/superpowers/specs/2026-07-07-standard-view-followups.md (first picks: popover Enter->\fp; caller-click popover open; human QA of the cell-click BCV chain; abandonment-window host-save resolve-all policy; LoadStatePlugin reload cluster). NOT pushed (push is a separate owner-approved step).
FINAL-REVIEW FIX WAVE: DONE (worktree a22d5f1eff shared marker-palette-keydown util EXTRACTED into platform-bible-react + popover capture-phase/selection-session port + papi filterText docs regen + passive height + chord guard both consumers + dist; library 16cfdc8 register triage: focusRequest CLOSED-by-d206e992, duplication CONFIRMED+CLOSED via extraction, filter-divergence sharpened, 4 new unfixed minors recorded). PBR 254 (12 util-matrix + 2 popover pins incl. in-session-Enter no-double-mutation), extension 210, renderer 25, all builds/tsc/lint clean. Bounded final confirmation dispatched.
FINAL CONFIRMATION (fable): READY TO MERGE — YES. Shared util branch-by-branch faithful (per-kind regexes byte-identical; deliberate chord-dismiss delta disclosed+registered, closes 2 recorded warts); popover capture+removal symmetric, mutually-exclusive activeElement guards (no double-handling); Enter pin discriminates 3 ways; papi JSDoc matches host (grep-clean); register edits match triage precisely; trailers exact throughout.
PHASE 5 COMPLETE — ALL 16 TASKS + 4 QA RUNS + 5 FIX WAVES + FINAL REVIEW DONE. Final HEADs: scripture-editors standard-view 16cfdc8; paranext worktree standard-view a22d5f1eff. NOT PUSHED (awaiting owner approval). Handoff: docs/superpowers/specs/2026-07-07-standard-view-followups.md (register), -phase5-notes.md, -pt9-ux-differences.md (PO), -simple-mode-impact.md.
